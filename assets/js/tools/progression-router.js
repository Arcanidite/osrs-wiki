// Entry point for the Progression Router tool page (loaded as an ES module
// via layout `tool_script`). Implementation lives in assets/js/router/:
// pure planner/graph/model modules + the editor/DOM layer.
import "../router/editor/index.js";
