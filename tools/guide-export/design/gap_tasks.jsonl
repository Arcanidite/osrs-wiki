{"id": "gap-ge-01", "gap": "D5 GE-buying leaked in firemaking methods (hard rule)", "track": "L1-hardrule", "kind": "FIX", "scope": {"left": "train_methods.jsonl rows train-firemaking-15/30/50/75/99 + steps.jsonl lines 58-62 (attached methods copies)", "right": "no other skills, no other rows; contribute-only, no repo edits"}, "micro_prompt": "You are gap-fixer gap-ge-01. FIX task: purge Grand-Exchange log sourcing from the firemaking method rows. HELD ROWS: grep '\"step_id\": \"train-firemaking-' /home/lemon/osrs-wiki/assets/data/tools/train_methods.jsonl > $OUT/rows.jsonl 2>&1, then Read it. Violations to fix (verified 2026-07-12): train-firemaking-30 methods[0] is named 'Oak logs from GE or Draynor oaks' (explicit GE sourcing — hard-rule violation); bands 15/50/75/99 list log methods whose only stated supply location is 'Grand Exchange (east side) or Varrock west bank' with no gather source. Rewrite policy (precedent: the smithing row in the same file already did this — grep '\"train-smithing-15\"' for its notes wording): method names/locations must name a gather/produce source (Woodcutting the named log at a named location per the wiki page 'Woodcutting' / 'Pay-to-play Firemaking training'), the burn spot may remain a bank row anywhere (say 'any long bank row, e.g. Varrock west bank' — do NOT name the GE as the identity of the method); if the page grounds no non-GE supply for a log tier, keep the method but add a note 'log supply: self-chopped (Woodcutting <lvl>); rate ??' — never a GE purchase. wikicli budget <=3 gets (pages are cached: check tools/wiki-kb/manifest.jsonl first). EMIT one contribution per band: key 'gapfix:ge:train-firemaking-<band>', kind 'gap-fix', body {step_id, methods: [full corrected methods[] array, same schema as held row], refs, notes}. methods[0] must still match the row's label/detail continuity AFTER gap-ge-02's detail fix ('Oak logs (chop Draynor oaks or self-supplied)'). xp_hr values: keep as held (sourced-or-??), never invent. Discipline: OUT=/tmp/gap-ge-01 (task-unique). Bash is WRITE-ONLY: every command redirects (cmd > $OUT/x 2>&1) then Read the file back. Read tools/wiki-kb/gotchas.log ([training] entries) + grep contrib.jsonl for existing 'gapfix:ge:' keys BEFORE starting. CLI=/home/lemon/osrs-wiki/tools/wiki-kb/wikicli: queue add+claim 'gap-ge-01' first, contribute --file per row (idempotent on key), queue done last. Own words only. Unknowns '??'. No repo data-file edits — the consolidator materializes. Append gotchas '[gapfix] ...' to tools/wiki-kb/gotchas.log; '## gap-ge-01 retro' (<=10 lines) to tools/wiki-kb/retro.log. Return a one-line receipt."}
{"id": "gap-ge-02", "gap": "D5 quest-imp-catcher parent detail recommends 'buy on GE' (hard rule)", "track": "L1-hardrule", "kind": "FIX", "scope": {"left": "steps_quests.jsonl row quest-imp-catcher + steps.jsonl train-firemaking-30 detail sentence", "right": "exactly these 2 rows; contribute-only"}, "micro_prompt": "You are gap-fixer gap-ge-02. FIX task, zero wiki fetches (rows hold everything). HELD ROWS: grep -F '\"quest-imp-catcher\"' /home/lemon/osrs-wiki/assets/data/tools/steps_quests.jsonl > $OUT/r1.json 2>&1; grep -F '\"train-firemaking-30\"' /home/lemon/osrs-wiki/assets/data/tools/steps.jsonl > $OUT/r2.json 2>&1; Read both. Violations: (1) imp-catcher detail says 'Collect the 4 coloured beads (buy on GE or kill imps ...)' — rewrite own-words to the gather-only path (kill imps for the four coloured beads; F2P imp spots near Falador's south entrance stay); note that its own sub-checklist already models the drop-gather (the oppgran bank has a gather atom flagging the GE bullet as excluded — keep that framing consistent). Beads-per-imp rate: '??'. (2) train-firemaking-30 detail 'Pick a method: Oak logs from GE or Draynor oaks / ...' — rewrite to 'Oak logs (chop Draynor oaks or self-supplied) / Coloured firelighter logs / Pyre logs' (must stay consistent with gap-ge-01's corrected methods[0] name). EMIT two contributions: keys 'gapfix:ge:quest-imp-catcher' and 'gapfix:ge:train-firemaking-30-detail', kind 'gap-fix', body {row_id, field: 'detail', new_detail, notes}. Copy refs question to notes only — refs untouched. Discipline: OUT=/tmp/gap-ge-02. Bash WRITE-ONLY (redirect then Read). Read gotchas.log + grep contrib.jsonl 'gapfix:ge:' first. CLI=/home/lemon/osrs-wiki/tools/wiki-kb/wikicli queue add+claim/contribute --file/queue done. Own words. '??' for unknowns. Gather/produce never GE. No repo edits. Gotcha+retro lines per standard. One-line receipt."}
{"id": "gap-ge-03", "gap": "D5/D11 consolidate GE fixes + suppress unreachable-anchor artifact, regen fixtures", "track": "L1-hardrule", "kind": "BUILD", "scope": {"left": "tools/guide-export/enrich.py emit guard + a new tools/consolidate_gap_fixes.py + regen of 5 fixtures", "right": "no planner (greedy/burndown/overlay) changes; no Java"}, "micro_prompt": "You are builder gap-ge-03 (single-owner, direct repo edits allowed). Depends on gap-ge-01/02 contributions existing in tools/wiki-kb/contrib.jsonl (grep 'gapfix:ge:' first; if absent, stop and report). Work in /home/lemon/osrs-wiki. (1) Write tools/consolidate_gap_fixes.py: read contrib rows keyed gapfix:ge:* and materialize them into assets/data/tools/train_methods.jsonl, steps.jsonl, steps_quests.jsonl (idempotent, re-runnable; match by row id; replace only the named field/array). (2) enrich.py emit guard: route-grand ships 'synth-tag-_quest_progression_anchor_unreachable-1' with instruction 'Obtain _quest_progression_anchor_unreachable' — a raw internal token, player-visible. At P11 emit, any synth step whose tag/label starts with '_' must be dropped from the emitted steps with a stderr lint line (honest, never silent: print the id). Grep enrich.py for 'synth-tag' to find the emitter. (3) Regenerate all five fixtures via the plan drivers (node tools/guide-export/plan-grand.mjs | python3 tools/guide-export/enrich.py > .../route-grand.json pattern — read each plan-*.mjs header for its exact invocation) into /home/lemon/runelite-guide-chain/src/main/resources/fixtures/. (4) Gates: npm test green (91/91 at baseline); re-run the GE negation-context sweep (python: find /grand exchange|\\bGE\\b/i contexts lacking a negation like 'non-GE|never|not the|west of the') over the five fixtures > $OUT/ge_after.txt and assert the imp-catcher + firemaking-30 leaks are gone; diff step-id lists before/after and assert the ONLY removals are _-prefixed synth artifacts. Baseline/fixture re-pins are intentional acts — note them in the commit body. Discipline: Bash write-only for verification output (redirect > file, Read back). Commit in two slices (consolidator+data, enrich+fixtures) on branch guide-export-pipeline; do not push without being asked. One-line receipt."}
{"id": "gap-idspace-01", "status": "resolved", "resolved_by": "Lane B — consolidate_quest_atoms.py short-id parent fallback + reconcile_quest_idspace.py (17 short-id quests re-registered from oppgran) + gap-rfd-01/02 (10 RFD chapters authored) + enrich.py quest_atoms-before-granular attach order", "resolution": "quest_atoms ALONE now covers 89/89 route-grand quest steps (was 79/89 split across two mechanisms); granular fallback is redundant for quests. gapfix:idmap:* contributions in contrib.jsonl; classification in assets/data/tools/quest_id_map.jsonl; analysis in design/gap-idspace-01-analysis.md. The '10 rfd ids have NO expansion anywhere' ground-truth was correct — they were the true ceiling, now closed by authoring not id-mapping.", "gap": "D7 two quest id-spaces cap grand sub-checklists at 79/89", "track": "L2-idspace", "kind": "CLASSIFY", "scope": {"left": "steps.jsonl quest/rfd ids (27) x steps_quests.jsonl ids (189) x quest_expansions coarse_ids (188) x coarse_expansions_oppgran coarse_ids (123)", "right": "read-only classification; no fetches; no data edits"}, "micro_prompt": "You are classifier gap-idspace-01. CLASSIFY task, zero wiki fetches. Build the quest id-space mapping matrix that Lane-2 workers will consume. Inputs (Read via redirected commands): python3 one-shot script > $OUT/matrix.json 2>&1 that loads /home/lemon/osrs-wiki/assets/data/tools/{steps.jsonl,steps_quests.jsonl,quest_expansions.jsonl,coarse_expansions_oppgran.jsonl} and emits, per quest id appearing in ANY space: {id, space: 'spine-short'|'quests-long'|'both', canonical_wiki_title_guess: from the row's own refs[].title (never invented; '??' if the row has no refs), has_questatoms: bool, has_oppgran: bool, routed_in_grand: bool (grep route-grand.json step ids), rfd: bool}. Known ground truth to verify, not assume: 27 spine-short ids exist (incl. all 10 rfd-*), 189 long ids, 62 overlap between oppgran and questatoms coarse ids, 10 rfd ids have NO expansion anywhere. EMIT one contribution per spine-short id: key 'gapfix:idmap:<short-id>', kind 'id-map', body {short_id, long_slug_equivalent: '<quest-...>' if the same quest exists in steps_quests under a different slug else null, wiki_title: from refs, needs_new_expansion: bool, notes}. Note: quest-mm is Monkey Madness I — steps_quests holds only quest-monkey-madness-ii, so most short ids have NO long twin (the spaces partition the corpus; verify per id, do not assume). Finish with a ranked depth-queue contribution key 'gapfix:idmap:depth-queue' listing the ids needing GENERATE work, RFD chapters first (they block the flagship's last 10 monoliths). Discipline: OUT=/tmp/gap-idspace-01. Bash WRITE-ONLY (redirect then Read). Read tools/wiki-kb/gotchas.log ([normalize-q], [quests]) + grep contrib.jsonl 'gapfix:idmap:' first. CLI queue add+claim 'gap-idspace-01', contribute --file, queue done. '??' for unknowns. No repo edits. Gotcha+retro. One-line receipt."}
{"id": "gap-rfd-01", "gap": "D7/D11 RFD 10 subquests are the flagship's last quest monoliths", "track": "L2-idspace", "kind": "GENERATE", "scope": {"left": "questatoms expansions for rfd-intro, quest-rfd-start, rfd-mountain-dwarf, rfd-goblins, rfd-pirate-pete", "right": "these 5 only; sibling task gap-rfd-02 owns the other 5"}, "micro_prompt": "You are quest-atomizer gap-rfd-01. GENERATE task: sub-checklists for 5 RFD steps: rfd-intro (+quest-rfd-start if distinct after checking both rows), rfd-mountain-dwarf, rfd-goblins, rfd-pirate-pete. HELD ROWS: grep -F '\"id\": \"rfd-' /home/lemon/osrs-wiki/assets/data/tools/steps.jsonl > $OUT/rows.jsonl 2>&1 and grep -F '\"quest-rfd-start\"' same file >> $OUT/rows.jsonl 2>&1; Read it — detail prose is cross-check only, never the source. FETCH (budget <=2 gets per subquest, most cached — grep tools/wiki-kb/manifest.jsonl for 'Recipe_for_Disaster' first): the RFD chapter pages; inherited trap (gotchas.log [normalize-q]): RFD chapters — one get --section 1 pulls the whole chapter; Quick-guide Details/Rewards H2s are transclusion stubs, never fetch them. EMIT one contribution per subquest, key 'questatoms:rfd-<slug>' (SAME namespace as the existing 188 — grep contrib.jsonl 'questatoms:' to confirm no collision), kind 'quest-expansion', schema identical to existing questatoms rows: {key, kind, quest_id: '<the steps.jsonl short id>', checkpoints: [{label, start_idx}], atoms: [{idx, verb, target, label, detail, count, cmp, until, reqs, consumes, produces, coords, refs, hints}], refs, notes}. Rules (GRANULARITY, closed enums — the consolidator will lint): verb in {talk-to walk-to teleport withdraw deposit buy sell kill gather produce use-on equip toggle plant harvest claim consume}; hint types ONLY in {do-while dialogue toggle-state batch-size teleport-choice rng-variance keep-drop safespot contested-fallback} (NO free-form 'note' types — put prose in detail); dialogue atomic, option routing = dialogue hint; repetition collapses to count/until; travel atoms only at zone transitions; coords ONLY from fetched {{Map}} pins (surface x 1000-4400 y 2200-3900); labels <=80 chars imperative, detail <=2 sentences OWN WORDS (never quote the walkthrough); every atom carries refs[] with manifest-verified slugs; unknowns '??'. Gather/produce never GE (RFD ingredient sourcing must be gather/produce/named-NPC-vendor). Discipline: OUT=/tmp/gap-rfd-01 (unique). Bash WRITE-ONLY. Read gotchas.log + existing keys first. CLI queue add+claim/contribute/queue done. Gotcha '[gapfix]' + retro. One-line receipt."}
{"id": "gap-rfd-02", "gap": "D7/D11 RFD remaining 5 subquests", "track": "L2-idspace", "kind": "GENERATE", "scope": {"left": "questatoms for rfd-evil-dave, rfd-skrach, rfd-sir-amik, rfd-awowogei, rfd-finale", "right": "these 5 only; gap-rfd-01 owns the first 5"}, "micro_prompt": "You are quest-atomizer gap-rfd-02. Identical spec to gap-rfd-01 (read its manifest row above in this file's design dir if in doubt — same schema, same closed enums, same traps) but scoped to: rfd-evil-dave, rfd-skrach, rfd-sir-amik, rfd-awowogei, rfd-finale. Held rows: grep -F '\"id\": \"rfd-' /home/lemon/osrs-wiki/assets/data/tools/steps.jsonl > $OUT/rows.jsonl 2>&1; Read; take only your 5. rfd-awowogei note: it gates on MM1 greegree routing — model the greegree as reqs/consumes on the atom that uses it, cite the chapter page, do NOT unwind MM1 here (that is quest-mm's own expansion). rfd-finale: the Culinaromancer fight atoms use kill verbs with until quest-varbit '??'. Keys 'questatoms:rfd-<slug>'. Budget <=2 gets per chapter (--section 1 pulls whole chapter, per the [normalize-q] gotcha). All discipline identical: OUT=/tmp/gap-rfd-02, Bash WRITE-ONLY (redirect then Read), read gotchas.log + existing 'questatoms:' keys first, queue add+claim/contribute --file/queue done, own words, '??' unknowns, closed verb/hint enums, coords only from {{Map}} pins, never GE, no repo edits, gotcha+retro lines, one-line receipt."}
{"id": "gap-shortid-01", "gap": "D7 17 spine short-id quests have no questatoms expansion", "track": "L2-idspace", "kind": "GENERATE", "scope": {"left": "questatoms for the 17 steps.jsonl short-id quests (quest-cooks-assistant ... quest-swan-song), bundled 6/6/5 across gap-shortid-01/02/03 in list order from gapfix:idmap:depth-queue", "right": "this task = first 6 in the depth-queue; do not touch long-slug quests (already covered)"}, "micro_prompt": "You are quest-atomizer gap-shortid-01. GENERATE task: questatoms expansions for the FIRST 6 short-id spine quests in the depth-queue contribution (grep contrib.jsonl for 'gapfix:idmap:depth-queue' > $OUT/dq.json 2>&1 and Read; if absent, fall back to this fixed list order: quest-cooks-assistant, quest-priest-in-peril, quest-nature-spirit, quest-druidic-ritual, quest-bone-voyage, quest-tale-of-arrav). IMPORTANT: 62 long-slug quests + these ids are ALSO covered by the oppgran bank at 26% citation — your output supersedes it, so citation is the point: every atom carries refs[] whose slug greps in tools/wiki-kb/manifest.jsonl. HELD ROWS: grep -F '\"id\": \"<qid>\"' /home/lemon/osrs-wiki/assets/data/tools/steps.jsonl > $OUT/<qid>.json per quest; the existing oppgran atoms are a STRUCTURE cross-check (grep coarse_of '\"<qid>\"' /home/lemon/osrs-wiki/assets/data/tools/steps_oppgran.jsonl > $OUT/<qid>-opp.jsonl) — reuse their good phase splits, never their uncited claims or their non-canon verbs (go-to/search/use/operate are BANNED; canonical 17 only). FETCH per quest <=5 gets (mostly cached): sections '<Quest>/Quick guide' then get Walkthrough --strip; traps inherited from [normalize-q]: transclusion-stub Details/Rewards H2s, no-quick-guide fallback to main-page Walkthrough, manifest-title-before-citing. Keys 'questatoms:<short-id>' (contrib namespace shared with the 188 — grep first, idempotent). Schema + closed enums + coords envelope + own-words + '??' + never-GE: identical to the standing questatoms spec (NORMALIZATION 3c). Checkpoints = own-words renames of walkthrough subsection headers with start_idx. Discipline: OUT=/tmp/gap-shortid-01 unique; Bash WRITE-ONLY; gotchas+keys read first; queue add+claim/contribute/done; gotcha '[gapfix]' + retro appended; one-line receipt."}
{"id": "gap-shortid-02", "gap": "D7 short-id quests bundle 2 of 3", "track": "L2-idspace", "kind": "GENERATE", "scope": {"left": "depth-queue items 7-12 (fallback list: quest-the-feud, quest-shadow-storm, quest-lost-city, quest-fairytale-1, quest-tai-bwo, quest-big-chompy)", "right": "these 6 only"}, "micro_prompt": "You are quest-atomizer gap-shortid-02. Identical spec, traps, schema, enums, discipline, and contribution shape to gap-shortid-01 (same design-file manifest row), scoped to depth-queue items 7-12 (fallback fixed list: quest-the-feud, quest-shadow-storm, quest-lost-city, quest-fairytale-1, quest-tai-bwo, quest-big-chompy). Keys 'questatoms:<short-id>'. OUT=/tmp/gap-shortid-02. Bash WRITE-ONLY; read gotchas.log + existing keys; queue add+claim/contribute --file/queue done; own words; '??'; canonical 17 verbs; closed hint enum; coords only from {{Map}}; never GE; no repo edits; gotcha+retro; one-line receipt."}
{"id": "gap-shortid-03", "gap": "D7 short-id quests bundle 3 of 3 (the big ones)", "track": "L2-idspace", "kind": "GENERATE", "scope": {"left": "depth-queue items 13-17 (fallback: quest-mm, quest-dt, quest-family-crest, quest-rum-deal, quest-swan-song)", "right": "these 5 only; MM1 and DT1 are grandmaster-length — budget accordingly"}, "micro_prompt": "You are quest-atomizer gap-shortid-03. Identical spec/traps/schema/enums/discipline to gap-shortid-01, scoped to: quest-mm (Monkey Madness I), quest-dt (Desert Treasure I), quest-family-crest, quest-rum-deal, quest-swan-song. These two grandmasters are long: fetch budget <=8 gets each (chapters/sections cached — grep manifest.jsonl first), atoms may reach 60-100 per quest — repetition still collapses (U4), travel atoms only at zone transitions (U6), phase-split on location boundaries with one checkpoint per region leg (Multiquest pattern). DT1 boss legs: kill atoms with until quest-varbit '??' + safespot hints only where the fetched page grounds them. Keys 'questatoms:<short-id>'. OUT=/tmp/gap-shortid-03. All standing discipline: Bash WRITE-ONLY, gotchas+keys first, queue add+claim/contribute/done, own words, '??', canonical verbs, closed hints, coord envelope, never GE, no repo edits, gotcha+retro, one-line receipt."}
{"id": "gap-prefer-01", "gap": "D1/D7 flagship renders the 26%-cited oppgran bank where the 100%-cited questatoms bank exists", "track": "L2-idspace", "kind": "BUILD", "scope": {"left": "tools/consolidate_quest_atoms.py (ingest new questatoms:* keys) + enrich.py attach preference + plan-grand.mjs comment + fixture regen", "right": "no oppgran deletion (append/annotate only); no Java"}, "micro_prompt": "You are builder gap-prefer-01 (single-owner builder, direct repo edits allowed). Preconditions (verify, stop+report if missing): contrib.jsonl has questatoms:rfd-* and questatoms:<short-id> keys from gap-rfd-01/02 + gap-shortid-01..03. Work in /home/lemon/osrs-wiki. (1) Run/extend tools/consolidate_quest_atoms.py so the new contributions mint into assets/data/tools/steps_quest_atoms.jsonl + quest_expansions.jsonl (ids q-<slug>-NN-<verb>-<target>, same linter: closed verb enum, checkpoint start resolves, coord envelope, refs slug-in-manifest — reject free-form hint types at lint, remapping is gap-enum-01's job for legacy rows only). The registry coarse_id for these = the steps.jsonl SHORT id (that is what route-grand steps carry). (2) Flip attach preference in enrich.py: currently (lines ~1256-1259) granular wins when both subChecklist sources exist; make questChecklist (quest_atoms) win over the oppgran subChecklist when BOTH are present — one-line swap of the two guard clauses, with a comment citing this task id. plan-grand.mjs keeps BOTH flags true (its task-#9 header explains why granular must stay until coverage is proven — update that comment to reflect the new preference). (3) Regen route-grand + route-quests; gates: grand quest subChecklist coverage must be 89/89 (python count: steps with id ^(quest|rfd)- and subChecklist non-null); grand subChecklist-atom refs coverage must exceed 95% (was 26%); route-p2p/corpus byte-identical (their flags are off); npm test green. Fixture re-pin = intentional act, note counts in the commit. Discipline: Bash write-only for all verification output (> $OUT file then Read). Commit in slices (data / enrich / fixtures). One-line receipt with the two coverage numbers."}
{"id": "gap-spine-01", "gap": "D6/D14 spine not superset: p2p 42 uniques, barrows contradiction, quest order two-truths", "track": "L3-spine", "kind": "BUILD", "scope": {"left": "tools/guide-export/plan-grand.mjs goal set + plan-multi.mjs DEFAULT_GOALS reference + route-grand/route-p2p regen", "right": "no fixture deletion yet (gap-spine-02 owns prune); no enrich.py changes"}, "micro_prompt": "You are builder gap-spine-01. Execute CHAIN_CONSOLIDATION.md section 4 step 3 (Read that doc first: /home/lemon/osrs-wiki/tools/guide-export/design/CHAIN_CONSOLIDATION.md). Task: extend plan-grand.mjs's goal set with the P2P milestone goals it lacks (quest-dt, quest-mm milestones, steer-ardougne-easy-diary, steer-graceful — read plan-multi.mjs DEFAULT_GOALS for the exact ids), re-bake route-grand, then re-run the overlap analysis (write a small spike modeled on tools/guide-export/spikes/lens-spike.mjs, or a python one-shot: step-id set comparison p2p vs grand > $OUT/overlap.txt) asserting route-p2p REAL uniques (steer/milestone/unlock/alternation kinds) -> 0. Stale train/synth band uniques may legitimately remain non-zero (different band cuts) — count and report them separately; do NOT hand-absorb bands. This also collapses the milestone-barrows contradiction (p2p 15 conds vs grand 10): the planner's single burndown answer wins — assert post-bake that grand's milestone-barrows conditions are whatever the bake emits and note the count change in the commit; NEVER hand-pick between the two condition sets. Gates: npm test green (91/91 baseline); route-quests/corpus/origin untouched byte-identical; route-grand re-pin intentional, commit message lists: new goals added, step count delta, barrows cond count, remaining p2p unique count by kind. Verify quest coverage did not regress: grand quest subChecklist count >= the pre-change count (python count > $OUT/cov.txt). Discipline: Bash write-only for verification (redirect > file, Read back). Branch guide-export-pipeline; commit; no push unless asked. One-line receipt with the unique-count table."}
{"id": "gap-spine-02", "gap": "D14 quest-cape epilogue: spine routes 89/215 quests; route-quests is a parallel truth", "track": "L3-spine", "kind": "BUILD", "scope": {"left": "plan-grand.mjs epilogue concat (goal-quest-cape residue after raids-cox) + route-grand regen + overlap gate", "right": "sequenced AFTER gap-spine-01; no manifest changes (gap-spine-03)"}, "micro_prompt": "You are builder gap-spine-02. Execute CHAIN_CONSOLIDATION.md section 4 step 4 (Read the doc + plan-grand.mjs header first — the header documents WHY goal-quest-cape was excluded: _difficulty episode-sort would demote CoX from the anchor spot). The fix is explicit placement, not sorting: CONCAT the remaining quest-cape residue AFTER the raids-cox terminal, exactly the same explicit-placement trick the origin prefix already uses at the front of the same file (grep plan-grand.mjs for the origin concat to copy the pattern). Residue = route-quests' quest steps not already on the spine, in route-quests' own baked order, with their woven training/supply steps. Expect the spine to grow to roughly 350-400 steps. Gates: (a) python overlap check > $OUT/superset.txt asserting every route-quests quest id is now IN route-grand (uniques -> ~0; report stragglers by name — quest-frozen-door-adjacent unroutables are acceptable, named); (b) the Quests lens over the new spine (reuse tools/guide-export/spikes/lens-spike.mjs) yields ONE canonical quest order — record LCS vs route-quests in the commit; (c) grand quest subChecklist coverage scales with the new quests (the questatoms attach is id-keyed — verify count ~= routed quest count minus known-uncovered, > $OUT/cov.txt); (d) npm test green; other fixtures byte-identical. Update plan-grand.mjs's header reasoning (append, never delete). Large intentional re-pin — commit body carries the before/after step counts and LCS. Discipline: Bash write-only verification; commit on guide-export-pipeline; one-line receipt with step-count delta + coverage."}
{"id": "gap-spine-03", "gap": "D6/D14 prune: P2P chain + orphaned fixtures (f2p-early-game.json, route-origin.json) + drift gate", "track": "L3-spine", "kind": "BUILD", "scope": {"left": "fixtures/manifest.json, deletion of route-p2p.json/route-origin.json/f2p-early-game.json, a checked-in overlap-gate spike, Library lens deep-links", "right": "AFTER gap-spine-01/02; keep plan-multi/plan-quests/plan-origin.mjs (assembly inputs + comparators)"}, "micro_prompt": "You are builder gap-spine-03. Execute CHAIN_CONSOLIDATION.md section 4 steps 5-6 (Read the doc first). In /home/lemon/runelite-guide-chain/src/main/resources/fixtures/: (1) manifest.json drops the p2p-progression entry (its content is absorbed per gap-spine-01; its navigational role is the Milestones lens) — final chains: Full Progression + Quest Reference (KEEP quest-progression only if gap-spine-02's superset left named stragglers; if uniques hit 0, drop it too and say so) + Full Corpus. (2) Delete route-p2p.json, route-origin.json (orphaned — zero manifest entries reference it today, verified 2026-07-12), and f2p-early-game.json (unreferenced; its name violates the no-F2P/P2P-split rule). (3) Library cards for retired chains become lens deep-links if the Library render references them (grep WebFragments.java for the retired chain ids > $OUT/lib.txt; adjust only what breaks). (4) Drift gate: promote the overlap script into a checked-in spike tools/guide-export/spikes/overlap-gate.mjs in the osrs-wiki repo that exits nonzero when a kept comparator bake has uniques vs the spine — wire nothing into CI, just make it runnable + document in the spike header. (5) Keep plan-multi.mjs/plan-quests.mjs/plan-origin.mjs — they are goal-set definitions + regression comparators, not shippable chains (their headers should say so; append a line). Gates: plugin compiles (gradle build or the project's standard check > $OUT/build.txt 2>&1); GuideWebMain standalone loads the remaining chains; npm test green in osrs-wiki. Two commits (one per repo), messages list every deleted file. Bash write-only verification. One-line receipt."}
{"id": "gap-m3-01", "gap": "D15 requisite gating surface (locked/available/done + needs-chips) designed-not-built", "track": "L4-render", "kind": "BUILD", "scope": {"left": "runelite-guide-chain: GuideStore.java (gate derivation + manual assumption toggles + persistence), PlanRow.java (+gate), WebFragments.java (dimmed row + needs/discrepancy chips), PanelOverlay", "right": "no planner/enrich changes; coordinate WebFragments edits with gap-reqblocks-01 (land sequentially)"}, "micro_prompt": "You are builder gap-m3-01. Build MATERIALIZATION.md section 2 + Lane M3 exactly as designed — Read /home/lemon/osrs-wiki/tools/guide-export/design/MATERIALIZATION.md sections 2 and 3/Lane-M3 first; its verify list is your acceptance block verbatim. Facts verified 2026-07-12: PlanRow.java currently has ONLY globalIndex/guideId/guideName/key/step/status — add nullable gate {state: 'locked'|'available', unmet: List<String>}; null renders exactly as today (zero churn). Derivation lives in GuideStore ONLY (single authority): done = existing checkState/auto-fire unchanged; available = not done AND every hard requisite satisfied (reqs.skills via CharacterSnapshot SKILL, quest gates via existing VARBIT conditions where sourced — '??' varbits degrade to MANUAL i.e. ungated, honest); locked = not done AND >=1 unmet. Offline: the same requisites render as tickable manual assumptions persisted with plan state; reconnect: live values WIN for display, manual assumptions kept, disagreement renders a discrepancy chip — never a silent overwrite in either direction. Invariants (equal-grade): locked rows render at the SAME rank, dimmed, with a 'needs: {first unmet}' chip — never hidden/indented/childed; a locked item stays manually checkable (advisory, not enforcement); uncheck never cascades. Verify per the doc: standalone GuideWebMain with a fixture step carrying an unmet SKILL renders locked+chip; manual check of a locked step works + persists; disconnect->toggles appear; reconnect-disagree->discrepancy chip; sideload via the launcher settings.json jvmArguments '-ea' recipe (project memory). All output via redirect > $OUT/* then Read (Bash write-only). Plugin repo branch feat/burndown-web-lane1; commit with the acceptance checklist ticked in the body. One-line receipt."}
{"id": "gap-reqblocks-01", "gap": "D10 REQUISITES/REWARDS/START blocks missing on step detail; D9 card start/length dropped", "track": "L4-render", "kind": "BUILD", "scope": {"left": "WebFragments.java step-detail fragment + ReferenceEntry.java (+start,+length) + GuideStep.java (+req_items,+branch)", "right": "sequence against gap-m3-01 on WebFragments (either order, both additive); no store logic"}, "micro_prompt": "You are builder gap-reqblocks-01. Close the render-parity gaps verified 2026-07-12: (1) ReferenceEntry.java (com/techdevgroup/guidechain/reference/) lacks start/length though catalog.jsonl carries them on 264/346 rows (start is an object {npc, location} on some rows — type Object, render defensively) — add both nullable fields + render in the referenceCard block layout (grep WebFragments.java for 'referenceCard'/'facts' to find the block pattern; REUSE it, don't fork). (2) GuideStep.java lacks req_items though route-grand carries it on 55 steps and route-quests on 160 — add List<ReqItem>-shaped nullable field (mirror ReferenceEntry.ReqItem: name/qty(Object)/note/optional) + a REQUISITES / REWARDS / START block on the STEP detail pane rendered from reqs (via completionConditions summaries), req_items, and consumes — NORMALIZATION.md section 3d item 2: the block is primary, the existing cond-badge row (WebFragments.java:310) demotes to a supplement, never deleted. (3) GuideStep also lacks branch{} (5 route-grand steps carry it: alt_group/when/optional) — add a nullable Branch field + a small 'alt: <alt_group>' chip; optional:true renders 'skippable'. All additive, unknown-field-safe Gson, no migration. Verify: GuideWebMain standalone against route-grand.json — pick step ids with req_items (grep the fixture > $OUT/ids.txt: e.g. any quest step with req_items) and confirm blocks render; a step without any of the fields renders byte-identical to today (diff the fragment HTML > $OUT/diff.txt). Bash write-only for verification. Plugin repo, branch feat/burndown-web-lane1, one commit. One-line receipt naming which fields now render."}
{"id": "gap-ownwords-01", "gap": "D2 ~9% of quest atoms carry >=8-word verbatim wiki phrases", "track": "L5-citation", "kind": "CLASSIFY", "scope": {"left": "ALL 5190 steps_quest_atoms.jsonl rows + the 416 cited steps_oppgran rows, vs their refs[] blobs in tools/wiki-kb/blobs/", "right": "mechanical scan only; zero fetches; no rewrites (workers downstream)"}, "micro_prompt": "You are classifier gap-ownwords-01. CLASSIFY task, zero wiki fetches — everything is on disk. Write a python one-shot (> $OUT/scan.log 2>&1) that: loads /home/lemon/osrs-wiki/assets/data/tools/steps_quest_atoms.jsonl and steps_oppgran.jsonl (rows with refs only); for each row builds the 8-gram word set of label+detail (lowercase, alpha tokens) and tests membership against the lowercased text of every blob file matching each refs[].slug base (tools/wiki-kb/blobs/<base>*.wiki — cache blobs in a dict); emits per HIT row {id, file, matched_gram, detail_len}. Calibration from this audit: seed 11, 80-row sample found 7 hits — expect roughly 400-500 hits corpus-wide. Then bundle hits by quest (coarse_of) into rewrite work-packets of <=25 atoms. EMIT: one contribution key 'gapfix:ownwords:index' with {total_scanned, total_hits, hits: [...]} (if too large for one row, shard 'gapfix:ownwords:index:NN' by quest initial) and one 'gapfix:ownwords:packet:<NN>' per work-packet {packet, atom_ids, quests, hit_grams}. Do NOT judge severity or rewrite anything — a hit is a hit; the rewrite wave owns wording. Discipline: OUT=/tmp/gap-ownwords-01; Bash WRITE-ONLY (script output > file, Read back); read gotchas.log + grep contrib.jsonl 'gapfix:ownwords:' first (idempotent — if index exists, verify and stop); CLI queue add+claim 'gap-ownwords-01'/contribute --file/queue done; no repo edits; gotcha '[gapfix]' + retro; one-line receipt with the hit count."}
{"id": "gap-ownwords-02", "gap": "D2 rewrite the flagged verbatim atoms own-words", "track": "L5-citation", "kind": "FIX", "scope": {"left": "one work-packet from gapfix:ownwords:packet:01 (<=25 atom ids); clone this row per packet NN at dispatch", "right": "flagged atoms only; labels/details only — never verbs, counts, coords, refs"}, "micro_prompt": "You are rewriter gap-ownwords-02 (packet 01 — the dispatcher clones this brief per packet). FIX task, zero wiki fetches. Pull your packet: grep -F 'gapfix:ownwords:packet:01' /home/lemon/osrs-wiki/tools/wiki-kb/contrib.jsonl > $OUT/packet.json 2>&1; Read it. For each atom id: grep -F '\"id\": \"<atom-id>\"' /home/lemon/osrs-wiki/assets/data/tools/steps_quest_atoms.jsonl > $OUT/<id>.json (or steps_oppgran.jsonl if not found); Read; rewrite label and/or detail so NO >=8-word run matches the cited blob (the packet lists the exact matched gram — that phrase must not survive), while preserving: the atom's meaning, verb/target/count/until untouched, refs untouched, coords untouched, <=80-char imperative label, <=2-sentence detail, '??' placeholders kept verbatim. Own words means restructure the sentence, not synonym-swap: state the action from the player's perspective in our voice. Never quote game dialogue. Self-check each rewrite: no 8 consecutive words shared with the matched gram's context (eyeball; the consolidator re-runs the scanner as the gate). EMIT one contribution per atom: key 'gapfix:ownwords:<atom-id>', kind 'gap-fix', body {atom_id, source_file, new_label (or null if unchanged), new_detail (or null), matched_gram}. Discipline: OUT=/tmp/gap-ownwords-02-p01 (unique per packet); Bash WRITE-ONLY; read gotchas.log + existing 'gapfix:ownwords:' keys first (skip already-contributed atoms — idempotent); queue add+claim 'gap-ownwords-02-p01'/contribute/done; no repo edits; gotcha+retro; one-line receipt '<n> atoms rewritten'."}
{"id": "gap-xp-01", "gap": "D3 synthetic train-* xp constants ship un-marked in all fixtures", "track": "L6-datatruth", "kind": "BUILD", "scope": {"left": "new tools/consolidate_xp.py + steps.jsonl train-*/ctr-*/synth xp fields + fixture regen", "right": "xp fields ONLY; no labels/details/methods; no planner code"}, "micro_prompt": "You are builder gap-xp-01. Execute the long-specified consolidate-xp pass (NORMALIZATION W0; gotcha [training] line 32 in tools/wiki-kb/gotchas.log documents the defect: bands starting above level 1 carry one copy-pasted placeholder constant reused across unrelated skills). Ground truth: the cached blob for the wiki Experience table — grep tools/wiki-kb/manifest.jsonl for 'Experience_table' > $OUT/slug.txt to get the exact blob name, Read the blob from tools/wiki-kb/blobs/. Write tools/consolidate_xp.py: parse the cumulative xp-per-level table from the blob; for every steps.jsonl row whose atom.until.skill or grants defines a band lo->hi (train-*, ctr-*, tsb-* rows with xp{} fields), recompute each xp entry as cumulative(hi) - cumulative(lo) where lo = the row's reqs.skills level for that skill (default 1); write the corrected value; rows where lo/hi cannot be derived mechanically keep their value UNCHANGED and get listed in $OUT/unresolved.txt (honest — no guessing a band). Idempotent + re-runnable; print a per-row before/after table > $OUT/xp_diff.txt. Then regen all fixtures whose drivers are affected (all five) and re-pin intentionally. Gates: npm test green (if planner baselines pin xp-derived orderings, the re-pin is intentional — note every order change in the commit body with the named cause 'consolidate-xp'); spot-verify 3 known-bad bands from the gotcha (e.g. a 40->60 band) against hand-computed cumulative deltas > $OUT/spot.txt. Commit slices: script / data / fixtures. Bash write-only verification. One-line receipt with rows-corrected count."}
{"id": "gap-enum-01", "gap": "D12 grammar drift: 471 non-enum hint types (questatoms) + 68 non-canon verbs (oppgran); no lint gate exists", "track": "L6-datatruth", "kind": "BUILD", "scope": {"left": "new tools/lint_atoms.py + mechanical remap of steps_quest_atoms.jsonl hints + steps_oppgran.jsonl verbs + fixture regen", "right": "mechanical remaps only — semantic content untouched; unmappable rows LISTED not guessed"}, "micro_prompt": "You are builder gap-enum-01. Two closed grammars are drifting (measured 2026-07-12): steps_quest_atoms.jsonl hints include 471 entries outside GRANULARITY section-4's closed enum ('note' 321, 'branch' 76, 'verb-gap' 40, 'item' 17, 'warning' 5, 'tip' 3, 'optional' 3, 'alt-method' 2, 'alt-route' 2, 'choice' 1, 'sequence-hint' 1); steps_oppgran.jsonl has 68 atoms with non-canon verbs (go-to 35, operate 15, use 10, search 8) + 21 non-enum hints. Read /home/lemon/osrs-wiki/tools/guide-export/design/granularity/GRANULARITY.md sections 1b and 4 first — the 17-verb list and 9 hint types are the law. Write tools/lint_atoms.py (checked in, re-runnable, exit nonzero on violation, per GRANULARITY section 6): verb-in-enum, hint-type-in-enum, checkpoints resolve, refs slug greps in manifest.jsonl, bare-number produces/consumes must be sourced-or-'??'. Then the mechanical remap (same script, --fix flag): verbs go-to->walk-to, search->gather, use->use-on, operate->toggle (verify each against the atom's own target/label before writing — mismatches go to $OUT/unmapped.txt, unchanged); hint types: 'note'/'tip'/'warning' -> fold value into the atom's detail tail (own-words, appended sentence) and drop the hint; 'branch'/'alt-method'/'alt-route'/'choice'/'optional' -> teleport-choice ONLY when the value is a transport list, else fold to detail + list in $OUT/branchy.txt for a human pass (these may deserve real branch{} — do NOT mint branch{} mechanically); 'item' -> fold to detail; 'verb-gap' (40) -> KEEP the information: fold to detail prefixed 'verb-gap:' and list in $OUT/verbgap.txt (they mark real grammar gaps — the retro should name any verb the enum genuinely lacks rather than silently flattening). Regen route-grand + route-quests; gates: lint exits 0 on both banks; npm test green; fixture diffs touch only hints/detail/verb fields (python field-level diff > $OUT/fielddiff.txt). Commit: script, then data+fixtures; body carries the remap counts. Bash write-only verification. One-line receipt."}
{"id": "gap-faults-01", "gap": "D13 4 source-after-consumer route faults live (volcanic_ash, coins@pps-04, ranarr_seed, raw_monkfish)", "track": "L6-datatruth", "kind": "FIX", "scope": {"left": "the 4 producer/consumer step pairs' ordering data (timing/reqs/tags) in steps.jsonl + supply_chains.jsonl", "right": "data-order fixes only — the design forbids silently reordering in code; no enrich/planner edits"}, "micro_prompt": "You are gap-fixer gap-faults-01. The backprop sweep (run 2026-07-12, node tools/guide-export/spikes/backprop-spike.mjs — re-run it yourself: node .../backprop-spike.mjs > $OUT/faults.txt 2>&1, Read it) flags 4 'source scheduled AFTER consumer' route faults on route-grand: volcanic_ash (consumer setup-ultracompost), coins (consumer pps-04-source-vials), ranarr_seed (consumer farm-ranarr-patch), raw_monkfish (consumer cook-monkfish). OPPORTUNISTIC_GRANULARITY section 2b is explicit: these are DATA faults to fix in the bank, never silently reordered by a pass. For each fault: locate producer + consumer rows (grep by id in /home/lemon/osrs-wiki/assets/data/tools/steps.jsonl > $OUT/<item>.json; supply_chains.jsonl for chain membership) and determine WHY the producer sorts late — usually a missing ahead-of-time timing, a missing tag:bootstrap-<chain> grant/req pair (SYNTHESIS S7's structural ordering trick), or a reqs.skills gate the consumer does not share. EMIT one contribution per fault: key 'gapfix:fault:<item>', kind 'gap-fix', body {item, producer_id, consumer_id, root_cause (one sentence), fix: {row_id, field, new_value} list — e.g. set producer timing 'ahead-of-time', or add grants tag:bootstrap-X on producer + reqs.tags on consumer}, and where the real answer is a missing earlier SOURCE (no producer exists before the consumer's zone-window), say so honestly: fix.proposed_new_step with full unified-schema row, produces '??' where unsourced, refs from wikicli (budget <=2 gets, cached). NEVER propose a GE purchase. Do not edit repo files — the consolidator applies + re-runs the spike as its gate (faults -> 0). Discipline: OUT=/tmp/gap-faults-01; Bash WRITE-ONLY; read gotchas.log + grep contrib.jsonl 'gapfix:fault:' first; queue add+claim/contribute --file/queue done; own words; '??'; gotcha+retro; one-line receipt."}
{"id": "gap-oppzones-01", "gap": "D13 7 no-window demands: unsourced zones block opportunistic weaves (food x2, ranarr_weed, guam_seed, compost, prayer_potion_4)", "track": "L6-datatruth", "kind": "GENERATE", "scope": {"left": "opp{}/zone grounding for the no-window items listed in the backprop spike output", "right": "these items only; contribution rows only (oppgran:opp:* namespace continues)"}, "micro_prompt": "You are researcher gap-oppzones-01. GENERATE task: the backprop sweep returns 'no-window' for 7 demands because their source rows lack zone/opp{} grounding (run node /home/lemon/osrs-wiki/tools/guide-export/spikes/backprop-spike.mjs > $OUT/plans.txt 2>&1 and Read — the no-window items at write time: food (ctr-05 + ctr-07a), ranarr_weed (brew-prayer-potion), guam_seed + compost (farm-herb-patch-guam), prayer_potion_4 (barrows horizon)). For each: identify the route's intended source step (grep produces '\"<item>\"' /home/lemon/osrs-wiki/assets/data/tools/steps.jsonl > $OUT/<item>-src.txt) and ground WHERE it is executable: zone slugs matching the location.zone vocabulary already in steps.jsonl, via wikicli (budget <=3 gets per item, most cached — manifest.jsonl first; the [npcs]/[items] title-alias gotchas apply). EMIT one contribution per item: key 'oppgran:opp:<item>@<zone>' (existing namespace — grep contrib.jsonl 'oppgran:opp:' first, idempotent), body {item, source_step, trigger: {zones: [...], hubs: []}, payoff_consumer, qty_or_rate: sourced-or-'??', refs, notes}. Where no wiki-groundable in-position window exists before the consumer, contribute the honest verdict {item, verdict: 'no-window-confirmed', why} — an honest no-window beats a guessed weave (OPPORTUNISTIC_GRANULARITY invariant 6). Gather/produce/named-NPC-vendor only, never GE. Discipline: OUT=/tmp/gap-oppzones-01; Bash WRITE-ONLY (redirect then Read); gotchas.log + existing keys BEFORE starting; queue add+claim/contribute --file/queue done; own words; '??'; no repo edits; gotcha '[gapfix]' + retro; one-line receipt."}
{"id": "gap-flags-01", "gap": "D3/D20 backlog: 57 contributor FLAGs + 79 lane5:* tickets + thin steer catalog (18/~60)", "track": "L6-datatruth", "kind": "CLASSIFY", "scope": {"left": "tools/wiki-kb/queue.jsonl lane5:* tickets + FLAG-annotated contrib rows", "right": "classification + ranked queue only; no fixes, no fetches"}, "micro_prompt": "You are classifier gap-flags-01. CLASSIFY task, zero wiki fetches. Inputs: grep 'lane5:' /home/lemon/osrs-wiki/tools/wiki-kb/queue.jsonl > $OUT/tickets.txt 2>&1; grep -i 'FLAG' /home/lemon/osrs-wiki/tools/wiki-kb/contrib.jsonl > $OUT/flags.txt 2>&1 (also grep retro.log for 'FLAG' summaries); Read both. Classify every ticket/FLAG into {mechanical-fix (held data states the correction), needs-wiki-fetch (a page must be read), needs-design-call (schema/model question)} and score IMPACT = how many routed fixture steps the wrong value touches (grep the affected row id across /home/lemon/runelite-guide-chain/src/main/resources/fixtures/route-*.json > $OUT/impact-<id>.txt; count occurrences). Known-live wrongs to make sure are captured (from the standing ledger): Nature Spirit / Swan Song / MM1 quest rewards, setup-ultracompost wrong on three axes, herblore 52->38 gate, unsourced prayer-43 gates. EMIT: 'gapfix:flags:index' {counts per class, total}; one 'gapfix:flags:bundle:<NN>' per <=15-item work bundle, ranked by impact score descending, each {bundle, class, items: [{ticket_or_key, affected_rows, impact, one_line_gap}]}. Also emit 'gapfix:flags:steer-catalog' listing the diary tiers absent from assets/data/tools/steer_points.jsonl (18 nodes today; SYNTHESIS S5 designs one node per diary tier ~60) as a GENERATE follow-on scope. Discipline: OUT=/tmp/gap-flags-01; Bash WRITE-ONLY; read gotchas.log + grep 'gapfix:flags:' first; queue add+claim/contribute/done; no repo edits; no fixes attempted; gotcha+retro; one-line receipt with the class counts."}
{"id": "gap-frames-01", "gap": "D19 frames breadth: 8/213 flagship steps carry media", "track": "L7-frames", "kind": "GENERATE", "scope": {"left": "first 20 route-grand steps lacking media[] whose scenes are world-only (no interface replay needed)", "right": "scenario-capture repo + step_media handoff; no Lane-S packets (separate task)"}, "micro_prompt": "You are capture worker gap-frames-01, operating in /home/lemon/scenario-capture (rev-236 engine; Read SME_NOTES.md section 15+ first) and reading fixtures from /home/lemon/runelite-guide-chain/src/main/resources/fixtures/route-grand.json. Pick the FIRST 20 steps in route order lacking media[] whose scene is world-only (player at a tile, NPCs/objects present — no mid-quest interface state): python listing > $OUT/candidates.txt (step id, zone, target npc/object from the atom/label), Read it, drop any needing interface/varbit replay (those wait on Lane S). Per step: author a scenario (existing scenario format — copy the pattern of a shipped one in spike/), run the oracle-gated capture (run script > $OUT/<step>.log 2>&1; oracle must pass — a frame without provenance does not ship, FRAMES_GALLERY hard rule), emit content-addressed artifacts media/<sha256[0:6]>/<name> and one step_media.jsonl handoff row per step {step_id, media: [{kind, path, caption (OWN WORDS, never wiki captions), state: {harness, rev, scenario, tile, varbits, captured}}]} — state{} is MANDATORY per entry, '??' members allowed, object never absent. Do NOT edit fixtures or enrich; the attach pass consumes the handoff (enrich media[] pass-through already ships). Idempotency: skip any step id already present in the existing step_media handoff or fixture media[] (grep first). Never run against a live client; loopback/offline harness only; no credentials. Discipline: Bash WRITE-ONLY (every run > file, Read back); read scenario-capture/gotchas.log first; append gotchas/retro there ('[frames] ...'); one-line receipt '<n>/20 captured, <m> skipped (interface-gated)'."}
{"id": "gap-lanes-01", "gap": "D19/D21 Lane S world-file injection unbuilt (interface/mid-quest states uncapturable)", "track": "L7-frames", "kind": "BUILD", "scope": {"left": "scenario-capture run_scenario.py --world world.json: LOC add, container update, VARP 56/95, IF_OPENSUB/IF_SETTEXT packets, decoder-sim tests", "right": "offline only; one packet at a time; no osrs-wiki/plugin repo changes"}, "micro_prompt": "You are builder gap-lanes-01 in /home/lemon/scenario-capture. Build BACKEND_REPLAY.md section 3b's Lane S — Read /home/lemon/osrs-wiki/tools/guide-export/design/BACKEND_REPLAY.md section 3 first, plus SME_NOTES.md for the rev-236 proto-server layout and the P6->P11 rsprox-lookup discipline (every packet's wire shape is verified against the rsprot/rsprox references BEFORE any client boot — grep the repo's rsprox-probe/ for the existing lookup pattern). Scope: run_scenario.py grows --world world.json (one declarative file replacing env-var creep) supporting, in build order, one packet at a time each proven by a decoder-sim test before the next: (1) LOC add (world object placement), (2) container update (inventory/bank contents), (3) VARP 56/95 writes (varbit-backed state), (4) IF_OPENSUB + IF_SETTEXT (interface open + text). Test pattern already established: tests/test_*_236.py — copy it; each new packet gets its own test that decodes the emitted bytes with the reference decoder and asserts field-level equality; NO live client, NO credentials, loopback only. Acceptance: a world.json fixture that places one LOC, fills one container slot, sets one VARP, opens one interface with set text, drives an Xvfb client boot to a captured frame whose oracle passes (> $OUT/accept.log 2>&1, Read it). Never automate input in-game; the harness drives a dummy offline client only. Commit per packet with its test. Bash write-only verification. One-line receipt listing packets landed."}
{"id": "gap-raids-01", "gap": "D20 raids are a bare milestone: no entry/prep sub-checklist (S10 stub #7)", "track": "L8-content", "kind": "GENERATE", "scope": {"left": "coarse expansions for boss/raid entry mechanics: Warriors' Guild -> Barrows -> GWD -> CoX prep+entry", "right": "entry/prep only (access, gear staging, mechanics overview atoms) — not fight-by-fight raid strategy"}, "micro_prompt": "You are content author gap-raids-01. GENERATE task: the route's endgame anchors (unlock-gwd, milestone-barrows, milestone-raids-cox — grep them in /home/lemon/runelite-guide-chain/src/main/resources/fixtures/route-grand.json > $OUT/anchors.txt) are bare cards; SYNTHESIS S10 stub #7 reserved 'boss/raid entry mechanics (Warriors Guild -> Barrows -> GWD)' and the registry row exists unauthored (grep coarse_expansions.jsonl for it > $OUT/reg.txt). Author entry/prep expansions at Faux grain for: warriors-guild-entry (token gathering loop, defender ladder as until-loops), barrows-entry (route in, per-brother crypt sequence as ONE checkpointed run atom set — repetition never unrolled), gwd-entry (kill-count mechanics, boss-room access), cox-entry (Woodcutting/Herblore prep atoms, party board, entry). Sources via wikicli (budget <=6 gets per target; grep manifest.jsonl first — several pages cached). Contribution per target: key 'questatoms:raid-<slug>' is WRONG namespace — use 'gapfix:raidprep:<slug>', kind 'coarse-expansion', body mirroring coarse_expansions.jsonl {coarse_id: '<anchor step id>', name, status: 'authored', steps: [full unified-schema atom rows inline], checkpoints: [{label, start}]} so the consolidator can mint rows + registry together. Rules: closed 17-verb enum + 9 hint types; consumes/produces sourced-or-'??' (supply demands like prayer_potion_4 quantities stay '??' or cite the page); gather/produce never GE; own words; every atom refs[] manifest-verified; overlay-only framing (guidance text never instructs automation). Discipline: OUT=/tmp/gap-raids-01; Bash WRITE-ONLY; gotchas.log + existing keys first; queue add+claim/contribute --file/queue done; no repo edits; gotcha+retro; one-line receipt."}
{"id": "gap-diaries-01", "gap": "D20 diaries: 48 cards + 3 steer nodes, zero routed task checklists", "track": "L8-content", "kind": "GENERATE", "scope": {"left": "diary-task sub-checklists for the tiers already steered (ardougne-easy, plus the tiers gap-flags-01's steer-catalog names first)", "right": "start with 4 tiers max; card facts stay untouched (track R owns cards)"}, "micro_prompt": "You are content author gap-diaries-01. GENERATE task: diaries exist as 48 reference cards + 3 steer nodes but no diary tier unwinds to a task checklist. Start with the tiers the route already anchors on: grep steer /home/lemon/osrs-wiki/assets/data/tools/steer_points.jsonl > $OUT/steers.txt 2>&1; take ardougne-easy + the next 3 tiers named by contrib key 'gapfix:flags:steer-catalog' if present (grep contrib.jsonl > $OUT/cat.txt) else pick the 3 earliest-route diary tiers from the catalog cards (kind 'diary' in runelite-guide-chain/src/main/resources/reference/catalog.jsonl). Per tier: fetch the diary page's task table via wikicli (budget <=3 gets/tier, cached likely — manifest.jsonl first); each TASK becomes one atom (verb from the closed 17-enum: gather/produce/talk-to/claim/toggle/kill/use-on; a task that is itself a quest completion is reqs.quests, not an atom); checkpoints group by area/activity clustering the page itself uses (own-words labels); final claim atom for the tier reward with the reward named own-words. Counts/rates sourced-or-'??'; coords only from {{Map}} pins; never GE for any task item; refs on every atom. Contribution per tier: key 'gapfix:diarytasks:<region>-<tier>', kind 'coarse-expansion', body {coarse_id: 'steer-<region>-<tier>-diary' (or the matching steer node id if it exists — say which), name, status: 'authored', steps: [inline atom rows], checkpoints}. Discipline: OUT=/tmp/gap-diaries-01; Bash WRITE-ONLY; gotchas.log ([diary] entries if any) + existing keys first; queue add+claim/contribute/done; own words; no repo edits; gotcha+retro; one-line receipt."}
{"id": "gap-backend-01", "gap": "D21 backend replay lanes P/A/L designed-not-built", "track": "L9-backend", "kind": "BUILD", "scope": {"left": "overlay-bridge service repo: protocol v2 additive subscribe/directives (P), QH-verbatim arrow package (A), observation ledger + fold (L)", "right": "low-risk lanes only; G/R/O explicitly deferred; guide-chain plugin/web view untouched"}, "micro_prompt": "You are builder gap-backend-01. Build BACKEND_REPLAY.md's three LOW-risk lanes in their design order — Read /home/lemon/osrs-wiki/tools/guide-export/design/BACKEND_REPLAY.md sections 1b (P), 4a (A), 2 (L) + section 5's lane table + /home/lemon/osrs-wiki/tools/overlay-bridge/PROTOCOL.md (v1, the additive base) before any code. Hard rules restated: overlay/highlight only — arrows guide the USER, never inject input; no real credentials; loopback/WG trust boundary only; thin client + restartable service (logic changes = service restart, never client relaunch). Lane P: v2 additive subscribe (skills, varbitChanged/varpChanged events per the doc's jsonc) — a v1 plugin against v2 must degrade gracefully (unknown ignored; test this explicitly); port discipline 43599 (43594 is the game port — collision gotcha logged). Lane A: replicate Quest Helper's arrow-waypoint rendering VERBATIM-mechanism (the doc names the exact mechanism — replicate, do not reinvent); offline acceptance = a static directive script drives arrows with no service logic. Lane L: append-only observation ledger, deduped/value-grouped/ordered by cache id per section 2a-2b; acceptance = fold-replay-twice-produces-zero-new-rows (idempotence proof, stated in section 5). Each lane owns its files per the section-5 table; one lane per commit; offline test before any live exposure. Service-side language free (Python asyncio per doc). Bash write-only verification (> $OUT then Read). One-line receipt naming which lanes landed + their acceptance evidence files."}
{"id": "gap-calib-01", "gap": "D16 cost model uncalibrated: COST_MODEL_V2 false, zero sourced est_minutes", "track": "L10-calibration", "kind": "BUILD", "scope": {"left": "tuning.js + enrich TUNING + est_minutes fills from measured data ONLY (Lane L ledger or manual measured runs)", "right": "GATED: do not start until a measurement source exists; never estimate prose"}, "micro_prompt": "You are builder gap-calib-01 — GATED task: verify a measurement source exists first (the Lane-L observation ledger from gap-backend-01 with step-window durations, or explicit user-provided measured runs); if neither exists, STOP and report 'gated: no telemetry' — do NOT proceed with estimates, the hard rule is never-fabricate. When unblocked: (1) write a consolidator (tools/ or service repo per where the ledger lives) that derives per-step wall-clock medians from ledger step-id windows and writes est_minutes ONLY on rows with >=3 observed windows (source annotation in a sibling field or commit note); (2) replace DEFAULT_STEP_MIN's placeholder 30 in /home/lemon/osrs-wiki/assets/js/router/planner/tuning.js with the measured median (keep the const name; update the comment from PLACEHOLDER to the measurement provenance) and mirror in enrich.py TUNING; (3) flip COST_MODEL_V2 true; (4) regression-diff every fixture: EVERY order change must be explainable by a named tuning constant — write the explanation table > $OUT/order_changes.txt and paste it into the commit body (baseline re-pins are intentional acts, per CLAUDE.md). npm test green. Bash write-only verification. One-line receipt with the measured DEFAULT_STEP_MIN and the count of steps gaining sourced est_minutes."}
