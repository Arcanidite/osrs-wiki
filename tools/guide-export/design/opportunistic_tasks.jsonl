{"id": "og-c1", "wave": 1, "tier": "haiku", "track": "granularize", "kind": "CLASSIFY", "scope": {"route": "/home/lemon/runelite-guide-chain/src/main/resources/fixtures/route-grand.json", "step_index": [1, 52], "first": "milestone-goal-early-game", "last": "quest-a-souls-bane"}, "micro_prompt": "CLASSIFY-ONLY probe (no deep dives, no wiki fetches, no code/data edits). Bash is WRITE-ONLY: every command redirects to a file (cmd > /tmp/og/c1-<n>.out 2>&1) which you Read back; you never receive stdout — missing output is the design. Before starting: grep /home/lemon/osrs-wiki/tools/wiki-kb/contrib.jsonl for 'oppgran:classify:s1' > /tmp/og/c1-claim.out 2>&1 and Read it — if the key exists, return receipt 'already-done' and stop. TASK: Read steps 1..52 (0-based slice [0:52]) of the route fixture named in scope via a python3 heredoc that writes a compact listing (id, instruction, presence of atom/hints/refs, detail first 200 chars) to /tmp/og/c1-steps.out. Classify EVERY step into exactly one grain_class: 'atomic' (already one interaction/count/until/toggle/bank-loadout per GRANULARITY Faux grain), 'train-band' (generalized Train X to N), 'quest-monolith' (single Complete-X card), 'travel-implicit' (movement buried in prose, no walk-to/teleport atom), 'supply-ok' (gather/produce row already at grain), or 'header' (milestone/chkpt/synth rows — skip further analysis). Also flag ge_violation:true when the detail recommends buying tradeables/market/GE (gather-not-GE hard rule), and has_refs:false when refs[] is missing/empty. For non-atomic rows name the applicable unwind rules from /home/lemon/osrs-wiki/tools/guide-export/design/granularity/GRANULARITY.md section 2 (e.g. ['U1','U5','U6']). OUTPUT: write ONE JSON file /tmp/og/c1-contrib.json shaped {\"key\": \"oppgran:classify:s1\", \"kind\": \"classify\", \"rows\": [{\"step_id\": \"...\", \"grain_class\": \"...\", \"unwind_rules\": [], \"ge_violation\": false, \"has_refs\": true}], \"notes\": \"<slice-level observations, own words>\"} then append it via: cd /home/lemon/osrs-wiki && tools/wiki-kb/wikicli contribute --file /tmp/og/c1-contrib.json > /tmp/og/c1-done.out 2>&1 (idempotent on key). Own words only; never copy fixture prose into notes. No fabrication: if a step is ambiguous, grain_class 'atomic' is NEVER the default — pick the closest generalized class and say why in notes. Return a ONE-LINE receipt: 'og-c1: 52 rows classified, N generalized, M ge-flags'."}
{"id": "og-c2", "wave": 1, "tier": "haiku", "track": "granularize", "kind": "CLASSIFY", "scope": {"route": "/home/lemon/runelite-guide-chain/src/main/resources/fixtures/route-grand.json", "step_index": [53, 104], "first": "quest-death-plateau", "last": "rfd-intro"}, "micro_prompt": "CLASSIFY-ONLY probe (no deep dives, no wiki fetches, no code/data edits). Bash is WRITE-ONLY: every command redirects to a file (cmd > /tmp/og/c2-<n>.out 2>&1) which you Read back; you never receive stdout. Before starting: grep /home/lemon/osrs-wiki/tools/wiki-kb/contrib.jsonl for 'oppgran:classify:s2' > /tmp/og/c2-claim.out 2>&1 and Read it — if present, return 'already-done' and stop. TASK: identical protocol to slice 1 but over steps 53..104 (0-based [52:104]) of the route fixture in scope: python3 heredoc writes the compact listing to /tmp/og/c2-steps.out; classify EVERY step into grain_class atomic|train-band|quest-monolith|travel-implicit|supply-ok|header; flag ge_violation (detail recommends buying tradeables/market/GE) and has_refs; cite applicable unwind rules U1-U10 from /home/lemon/osrs-wiki/tools/guide-export/design/granularity/GRANULARITY.md section 2 for non-atomic rows. This slice is train-heavy (15 train-* rows) — note for each train row whether it has NO atom at all vs a single kill/gather-until atom missing the surrounding Faux sequence (bank loadout, walk-to, gear stop); use notes to distinguish, both classify as 'train-band'. OUTPUT: ONE JSON file /tmp/og/c2-contrib.json shaped {\"key\": \"oppgran:classify:s2\", \"kind\": \"classify\", \"rows\": [{\"step_id\", \"grain_class\", \"unwind_rules\", \"ge_violation\", \"has_refs\"}...], \"notes\": \"own words\"} appended via cd /home/lemon/osrs-wiki && tools/wiki-kb/wikicli contribute --file /tmp/og/c2-contrib.json > /tmp/og/c2-done.out 2>&1. Own words only; no fabrication — ambiguous rows get the closest generalized class, never default-atomic. ONE-LINE receipt: 'og-c2: 52 rows, N generalized, M ge-flags'."}
{"id": "og-c3", "wave": 1, "tier": "haiku", "track": "granularize", "kind": "CLASSIFY", "scope": {"route": "/home/lemon/runelite-guide-chain/src/main/resources/fixtures/route-grand.json", "step_index": [105, 156], "first": "quest-fishing-contest", "last": "quest-the-giant-dwarf"}, "micro_prompt": "CLASSIFY-ONLY probe (no deep dives, no wiki fetches, no code/data edits). Bash is WRITE-ONLY: every command redirects to a file (cmd > /tmp/og/c3-<n>.out 2>&1) which you Read back; you never receive stdout. Before starting: grep /home/lemon/osrs-wiki/tools/wiki-kb/contrib.jsonl for 'oppgran:classify:s3' > /tmp/og/c3-claim.out 2>&1 and Read it — if present, return 'already-done' and stop. TASK: identical protocol to slice 1 but over steps 105..156 (0-based [104:156]) of the route fixture in scope: python3 heredoc writes the compact listing to /tmp/og/c3-steps.out; classify EVERY step into grain_class atomic|train-band|quest-monolith|travel-implicit|supply-ok|header; flag ge_violation (detail recommends buying tradeables/market/GE) and has_refs; cite applicable unwind rules U1-U10 from /home/lemon/osrs-wiki/tools/guide-export/design/granularity/GRANULARITY.md section 2 for non-atomic rows. This is the densest generalized slice (24 train-* + 21 quest-* rows): expect most rows to classify train-band or quest-monolith; for quest rows note in the row whether the quest is multi-region (its detail names 2+ regions) since multi-region monoliths get phase-split priority downstream. OUTPUT: ONE JSON file /tmp/og/c3-contrib.json shaped {\"key\": \"oppgran:classify:s3\", \"kind\": \"classify\", \"rows\": [{\"step_id\", \"grain_class\", \"unwind_rules\", \"ge_violation\", \"has_refs\", \"multi_region\": false}...], \"notes\": \"own words\"} appended via cd /home/lemon/osrs-wiki && tools/wiki-kb/wikicli contribute --file /tmp/og/c3-contrib.json > /tmp/og/c3-done.out 2>&1. Own words only; no fabrication. ONE-LINE receipt: 'og-c3: 52 rows, N generalized, M ge-flags'."}
{"id": "og-c4", "wave": 1, "tier": "haiku", "track": "granularize", "kind": "CLASSIFY", "scope": {"route": "/home/lemon/runelite-guide-chain/src/main/resources/fixtures/route-grand.json", "step_index": [157, 205], "first": "quest-the-hand-in-the-sand", "last": "milestone-raids-cox"}, "micro_prompt": "CLASSIFY-ONLY probe (no deep dives, no wiki fetches, no code/data edits). Bash is WRITE-ONLY: every command redirects to a file (cmd > /tmp/og/c4-<n>.out 2>&1) which you Read back; you never receive stdout. Before starting: grep /home/lemon/osrs-wiki/tools/wiki-kb/contrib.jsonl for 'oppgran:classify:s4' > /tmp/og/c4-claim.out 2>&1 and Read it — if present, return 'already-done' and stop. TASK: identical protocol to slice 1 but over steps 157..205 (0-based [156:205], 49 steps) of the route fixture in scope: python3 heredoc writes the compact listing to /tmp/og/c4-steps.out; classify EVERY step into grain_class atomic|train-band|quest-monolith|travel-implicit|supply-ok|header; flag ge_violation (detail recommends buying tradeables/market/GE) and has_refs; cite applicable unwind rules U1-U10 from /home/lemon/osrs-wiki/tools/guide-export/design/granularity/GRANULARITY.md section 2 for non-atomic rows. This slice mixes late quest monoliths with already-atomized rfd-*/pps-*/ctr-* rows — verify the atomized rows actually meet the Faux-grain bar (one interaction/count/until/toggle/bank-loadout per atom) instead of rubber-stamping them 'atomic'; note any that carry multiple interactions in one atom. For quest rows record multi_region as in slice 3. OUTPUT: ONE JSON file /tmp/og/c4-contrib.json shaped {\"key\": \"oppgran:classify:s4\", \"kind\": \"classify\", \"rows\": [{\"step_id\", \"grain_class\", \"unwind_rules\", \"ge_violation\", \"has_refs\", \"multi_region\": false}...], \"notes\": \"own words\"} appended via cd /home/lemon/osrs-wiki && tools/wiki-kb/wikicli contribute --file /tmp/og/c4-contrib.json > /tmp/og/c4-done.out 2>&1. Own words only; no fabrication. ONE-LINE receipt: 'og-c4: 49 rows, N generalized, M ge-flags'."}
{"id": "og-c5", "wave": 1, "tier": "haiku", "track": "opportunistic-map", "kind": "CLASSIFY", "scope": {"files": ["/home/lemon/osrs-wiki/assets/data/tools/steps.jsonl", "/home/lemon/osrs-wiki/assets/data/tools/goals.jsonl", "/home/lemon/osrs-wiki/assets/data/tools/supply_chains.jsonl"], "bounds": "grep-only over these three files; no fixture, no wiki fetches"}, "micro_prompt": "CLASSIFY-ONLY opportunity-surface probe (grep/python over local files; NO wiki fetches, NO deep dives, NO edits). Bash is WRITE-ONLY: every command redirects to a file (cmd > /tmp/og/c5-<n>.out 2>&1) which you Read back; you never receive stdout. Before starting: grep /home/lemon/osrs-wiki/tools/wiki-kb/contrib.jsonl for 'oppgran:opp-surface' > /tmp/og/c5-claim.out 2>&1 and Read it — if present, return 'already-done' and stop. TASK: enumerate every DOWNSTREAM REQUISITE in the demand graph: (1) python3 heredoc over the three scope files writing /tmp/og/c5-demand.out: all item slugs appearing in any steps.jsonl consumes{}, any goals.jsonl reqs.items, any supply_chains.jsonl output_item; for each item record its consumers (step ids whose consumes{} name it, goal ids whose reqs.items name it). (2) For each demanded item find candidate SOURCE rows: steps.jsonl rows whose produces{} name it; record source_step id and its location.zone/region and hub if set; when no producing row exists record source_step null and source_zone \"??\" — that is a real gap, not an error (per the [bg] gotcha: missing row may need a PROPOSED-ID later, not a skip). OUTPUT: ONE JSON file /tmp/og/c5-contrib.json shaped {\"key\": \"oppgran:opp-surface\", \"kind\": \"classify\", \"rows\": [{\"item\": \"slug\", \"consumers\": [\"step-or-goal-id\"], \"source_step\": \"id-or-null\", \"source_zone\": \"zone-or-??\", \"qty_demanded\": \"n-or-??\"}], \"notes\": \"own words: which items have plausible early in-position windows (source zone appears early in progression) vs late-only\"} appended via cd /home/lemon/osrs-wiki && tools/wiki-kb/wikicli contribute --file /tmp/og/c5-contrib.json > /tmp/og/c5-done.out 2>&1. Never invent zones or quantities — \"??\" beats a guess. ONE-LINE receipt: 'og-c5: N demanded items, M with known source, K gaps'."}
{"id": "og-c6", "wave": 1, "tier": "haiku", "track": "opportunistic-map", "kind": "CLASSIFY", "scope": {"files": ["/home/lemon/osrs-wiki/tools/wiki-kb/blobs/Guide_Ironman_Multiquest.s2.wiki", "/home/lemon/osrs-wiki/tools/wiki-kb/blobs/Guide_Leagues__Faux_starting_guide.s3.wiki", "/home/lemon/osrs-wiki/tools/wiki-kb/blobs/Guide_Leagues__Faux_starting_guide.s7.wiki"], "bounds": "cached blobs only; ZERO wikicli get calls"}, "micro_prompt": "PATTERN-EXTRACTION probe over CACHED exemplar blobs (Read the three scope files directly from disk; ZERO wikicli fetches — everything is already cached). Bash is WRITE-ONLY: every command redirects to a file you Read back. Before starting: grep /home/lemon/osrs-wiki/tools/wiki-kb/contrib.jsonl for 'oppgran:patterns' > /tmp/og/c6-claim.out 2>&1 and Read it — if present, return 'already-done' and stop. Read /home/lemon/osrs-wiki/tools/wiki-kb/gotchas.log lines tagged [multiquest-im] and [faux] first. TASK: extract the opportunistic-weave STRUCTURE (never the prose — own words only, hard rule): catalogue every instance of these pattern families in the blobs: (a) during-X-do-Y (an action for a LATER quest/goal performed inside the current activity's location), (b) buy-batch-for-later (one vendor/shop visit stocking requisites for MULTIPLE downstream consumers), (c) pickup-en-route (ground/scenery item grabbed while passing), (d) progress-until-location (a quest advanced only to a location-keyed boundary then parked), (e) leg-loadout-union (bank withdraw naming items for several downstream activities in one region leg). For each instance record: pattern family, the trigger (what makes the player in-position: zone/shop/quest-phase), the payoff target (which later quest/activity consumes it), and a paraphrased one-line description IN YOUR OWN WORDS. OUTPUT: ONE JSON file /tmp/og/c6-contrib.json shaped {\"key\": \"oppgran:patterns\", \"kind\": \"pattern\", \"rows\": [{\"family\": \"during-x-do-y\", \"trigger\": \"...\", \"payoff\": \"...\", \"paraphrase\": \"own words\"}], \"refs\": [{\"title\": \"Guide:Ironman Multiquest\", \"url\": \"https://oldschool.runescape.wiki/w/Guide:Ironman_Multiquest\"}, {\"title\": \"Guide:Leagues: Faux starting guide\", \"url\": \"https://oldschool.runescape.wiki/w/Guide:Leagues:_Faux_starting_guide\"}], \"notes\": \"which families dominate; counts per family\"} appended via cd /home/lemon/osrs-wiki && tools/wiki-kb/wikicli contribute --file /tmp/og/c6-contrib.json > /tmp/og/c6-done.out 2>&1. NEVER quote a bullet verbatim into rows — paraphrase or the contribution is invalid. ONE-LINE receipt: 'og-c6: N instances across 5 families'."}
{"id": "og-p1", "wave": 2, "tier": "sonnet", "track": "weave", "kind": "CLASSIFY", "scope": {"inputs": ["contrib keys oppgran:classify:s1..s4", "oppgran:opp-surface", "oppgran:patterns"], "outputs": "queue keys oppgran:depth:01..NN + contrib oppgran:depth-queue"}, "micro_prompt": "PRIORITIZER (reads ledger rows, emits ranked depth queue; NO wiki fetches, NO atomizing, NO edits outside ledgers). Bash is WRITE-ONLY: every command redirects to a file you Read back. Before starting: Read /home/lemon/osrs-wiki/tools/wiki-kb/gotchas.log entries tagged [prioritize] and [enqueue3] — they document exactly the duplicate-work traps this task must avoid. TASK: (1) extract the six wave-1 contributions from /home/lemon/osrs-wiki/tools/wiki-kb/contrib.jsonl (grep keys oppgran:classify:s1..s4, oppgran:opp-surface, oppgran:patterns into /tmp/og/p1-in.out and Read). (2) Build the granularize backlog: every step classified train-band|quest-monolith|travel-implicit, ordered by ROUTE POSITION (earliest first — early-route grain compounds downstream). CROSS-DIFF before bundling: drop any step whose atoms already exist (grep steps.jsonl for coarse_of:<step_id> and contrib.jsonl for oppgran:atoms:<step_id>); flag ge_violation rows as kind FIX inside their bundle. Bundle at most 12 step ids per depth key. (3) Build the opportunity shortlist from oppgran:opp-surface: keep items whose source_zone plausibly appears in the route EARLIER than their consumer (use the classify slices' step ordering as the route map); rank by (route-distance saved, consumer criticality: quest-blocking > goal reqs > supply top-up). (4) Emit: for each bundle, cd /home/lemon/osrs-wiki && tools/wiki-kb/wikicli queue add oppgran:depth:NN '{\"note\": \"<bundle summary>\", \"step_ids\": [...], \"track\": \"granularize|opportunistic-map\"}' > /tmp/og/p1-qNN.out 2>&1; then ONE contribution key oppgran:depth-queue with the full ranked table {\"key\": \"oppgran:depth-queue\", \"kind\": \"plan\", \"bundles\": [{\"queue_key\", \"track\", \"step_ids\"|\"items\", \"rank\", \"why\"}], \"notes\": \"own words\"} via contribute --file. Wave-3 manifest rows og-g1..og-g10 and og-o1..o2 already pre-cordon segments — your queue REFINES their id lists (prunes done/dup work, confirms order); do not invent new segment shapes. ONE-LINE receipt: 'og-p1: N depth bundles queued, M steps pruned as done, K opportunities shortlisted'."}
{"id": "og-g1", "wave": 3, "tier": "sonnet", "track": "granularize", "kind": "GENERATE", "scope": {"bundle": "quest monoliths, route order, segment 1 of 6", "first": "quest-sheep-shearer", "last": "quest-goblin-diplomacy", "count": 12, "bounds": "ONLY quest-* steps without atom{} between first and last inclusive in route-grand order; enumerate from the fixture; cross-check queue oppgran:depth:* for pruned ids"}, "micro_prompt": "ATOMIZE quest monoliths (GENERATE track; contribute rows, NEVER edit steps.jsonl or fixtures — consolidation applies later). Bash is WRITE-ONLY: every command redirects to a file (cmd > /tmp/og/g1-<n>.out 2>&1) which you Read back. Before starting: (1) Read /home/lemon/osrs-wiki/tools/wiki-kb/gotchas.log entries tagged [quests], [items], [depth:02], [depth:07] — the Quick-guide-stub, title-redirect and flatten-the-chain traps are all documented there; (2) grep contrib.jsonl for oppgran:atoms: to skip claimed work; (3) Read the queue payload for your bundle (tools/wiki-kb/wikicli queue list > /tmp/og/g1-q.out 2>&1, grep oppgran:depth) — it may prune ids; (4) queue claim your bundle key. SCOPE: the quest-* steps WITHOUT atom{} from quest-sheep-shearer to quest-goblin-diplomacy inclusive in route-grand order (enumerate via python3 over /home/lemon/runelite-guide-chain/src/main/resources/fixtures/route-grand.json into /tmp/og/g1-ids.out). PER QUEST: fetch ground truth via cd /home/lemon/osrs-wiki && tools/wiki-kb/wikicli — sections '<Quest>' first, then get '<Quest>' --section <Details-idx> --strip (real reqs/start/items; the /Quick guide Details H2 is ALWAYS a stub), then sections '<Quest>/Quick guide' and get its Walkthrough sections --strip (step structure). Unwind per /home/lemon/osrs-wiki/tools/guide-export/design/OPPORTUNISTIC_GRANULARITY.md section 1b and GRANULARITY.md rules: one talk-to atom per NPC state change (U5), gather/produce atoms for quest items with exact counts (U2, gather-not-GE U3 — if the wiki route says buy tradeables, model the gather/produce alternative and note the divergence), walk-to only at zone transitions (U6), multi-region quests phase-split on location.quest_phase boundaries with one checkpoint per region leg (U1), branch{} for genuine alternates (U9), ids qXX-NN-verb-slug with coarse_of=<quest-step-id> (U10). Every count sourced or \"??\"; every atom row carries refs[] citing the exact section-suffixed manifest slug you fetched (grep manifest.jsonl for the real recorded title first — title-redirect trap). OUTPUT: one contribution PER QUEST: {\"key\": \"oppgran:atoms:<quest-step-id>\", \"kind\": \"atoms\", \"coarse_id\": \"<quest-step-id>\", \"steps\": [<full steps.jsonl-shaped atom rows>], \"checkpoints\": [{\"label\", \"start\"}], \"refs\": [...], \"notes\": \"own words\"} via contribute --file. OWN WORDS ONLY in every label/detail — never copy wiki prose or dialogue. queue done your bundle key with a summary payload; append one gotchas.log line if you hit a new trap. ONE-LINE receipt: 'og-g1: N quests atomized, M atoms, K \"??\" placeholders'."}
{"id": "og-g2", "wave": 3, "tier": "sonnet", "track": "granularize", "kind": "GENERATE", "scope": {"bundle": "quest monoliths, route order, segment 2 of 6", "first": "quest-hazeel-cult", "last": "quest-rag-and-bone-man-i", "count": 12, "bounds": "ONLY quest-* steps without atom{} between first and last inclusive in route-grand order"}, "micro_prompt": "ATOMIZE quest monoliths, segment 2 of 6 — identical protocol to og-g1 (same discipline: Bash write-only with > /tmp/og/g2-<n>.out 2>&1 redirects; read gotchas [quests]/[items]/[depth:02]/[depth:07] first; grep contrib.jsonl oppgran:atoms: for claimed work; queue claim/done your oppgran:depth bundle; contribute rows only, never edit steps.jsonl/fixtures). SCOPE: quest-* steps WITHOUT atom{} from quest-hazeel-cult to quest-rag-and-bone-man-i inclusive in route-grand order (enumerate from /home/lemon/runelite-guide-chain/src/main/resources/fixtures/route-grand.json). Follow OPPORTUNISTIC_GRANULARITY.md section 1b + GRANULARITY.md U1-U10: talk-to per state change, exact-count gather/produce (gather-not-GE; wiki buy-routes get a modeled gather alternative + divergence note), walk-to at zone transitions only, phase-split multi-region quests on location.quest_phase with per-leg checkpoints, branch{} for alternates, ids per U10 with coarse_of FK. Wiki protocol per quest: sections '<Quest>' then get main-page Details --strip (never the /Quick guide Details stub), then /Quick guide Walkthrough sections --strip; cite the exact section-suffixed manifest slug (grep manifest.jsonl for the recorded title — redirect trap); flatten nested quest-requirement chains fully per the [depth:02] convention. Every count/coord sourced or \"??\" — never guessed. OWN WORDS ONLY in labels/details. OUTPUT: one contribution per quest keyed oppgran:atoms:<quest-step-id> shaped {key, kind: \"atoms\", coarse_id, steps: [steps.jsonl-shaped atom rows], checkpoints, refs, notes} via contribute --file. Append a gotchas.log line for any new trap. ONE-LINE receipt: 'og-g2: N quests, M atoms, K placeholders'."}
{"id": "og-g3", "wave": 3, "tier": "sonnet", "track": "granularize", "kind": "GENERATE", "scope": {"bundle": "quest monoliths, route order, segment 3 of 6", "first": "quest-the-fremennik-trials", "last": "quest-the-golem", "count": 12, "bounds": "ONLY quest-* steps without atom{} between first and last inclusive in route-grand order"}, "micro_prompt": "ATOMIZE quest monoliths, segment 3 of 6 — identical protocol to og-g1 (Bash write-only via > /tmp/og/g3-<n>.out 2>&1; read gotchas [quests]/[items]/[depth:02]/[depth:07]; grep contrib.jsonl oppgran:atoms: first; queue claim/done your oppgran:depth bundle; contribute only, no direct edits). SCOPE: quest-* steps WITHOUT atom{} from quest-the-fremennik-trials to quest-the-golem inclusive in route-grand order (enumerate from /home/lemon/runelite-guide-chain/src/main/resources/fixtures/route-grand.json). This segment is multi-region-heavy (Fremennik/Karamja/desert legs) — phase-split on location.quest_phase boundaries is the primary lever here: park a quest at its travel boundary exactly like the Multiquest progress-until-location pattern documented in contrib key oppgran:patterns (grep it first), one checkpoint per region leg (U1), and record each leg's zone in location.zone so the wave-4 opportunistic scan can find in-position windows inside your phases. All other rules identical: U5 dialogue atoms, U2/U3 exact-count gather-not-GE, U6 travel at transitions only, branch{} U9, ids U10 with coarse_of. Wiki: main-page Details --strip + /Quick guide Walkthrough --strip; manifest-slug-verified refs on every row; counts/coords \"??\" when unsourced. OWN WORDS ONLY. OUTPUT: one contribution per quest keyed oppgran:atoms:<quest-step-id> {key, kind: \"atoms\", coarse_id, steps, checkpoints, refs, notes} via contribute --file. ONE-LINE receipt: 'og-g3: N quests, M atoms, K placeholders'."}
{"id": "og-g4", "wave": 3, "tier": "sonnet", "track": "granularize", "kind": "GENERATE", "scope": {"bundle": "quest monoliths, route order, segment 4 of 6", "first": "quest-elemental-workshop-i", "last": "quest-the-giant-dwarf", "count": 12, "bounds": "ONLY quest-* steps without atom{} between first and last inclusive in route-grand order"}, "micro_prompt": "ATOMIZE quest monoliths, segment 4 of 6 — identical protocol to og-g1 (Bash write-only via > /tmp/og/g4-<n>.out 2>&1; read gotchas [quests]/[items]/[depth:02]/[depth:07]; grep contrib.jsonl oppgran:atoms: first; queue claim/done your oppgran:depth bundle; contribute only, no direct edits). SCOPE: quest-* steps WITHOUT atom{} from quest-elemental-workshop-i to quest-the-giant-dwarf inclusive in route-grand order (enumerate from /home/lemon/runelite-guide-chain/src/main/resources/fixtures/route-grand.json). Rules: OPPORTUNISTIC_GRANULARITY.md 1b + GRANULARITY.md U1-U10 (talk-to per state change; exact-count gather/produce, gather-not-GE with modeled alternatives for wiki buy-routes; walk-to at zone transitions only; multi-region phase-split on location.quest_phase; branch{} alternates; U10 ids + coarse_of FK). Wiki per quest: sections then main-page Details --strip (never the /Quick guide Details stub) then /Quick guide Walkthrough --strip; refs cite the exact section-suffixed manifest slug (grep manifest.jsonl for the recorded title first); flatten nested requirement chains per [depth:02]. Quest-item consumes{} must name slugs so the opportunistic scan can bind demand edges — a quest item without a consumes entry is invisible to lever B; \"??\" quantities allowed, missing edges not. OWN WORDS ONLY; counts/coords sourced or \"??\". OUTPUT: one contribution per quest keyed oppgran:atoms:<quest-step-id> {key, kind: \"atoms\", coarse_id, steps, checkpoints, refs, notes} via contribute --file. ONE-LINE receipt: 'og-g4: N quests, M atoms, K placeholders'."}
{"id": "og-g5", "wave": 3, "tier": "sonnet", "track": "granularize", "kind": "GENERATE", "scope": {"bundle": "quest monoliths, route order, segment 5 of 6", "first": "quest-the-hand-in-the-sand", "last": "quest-temple-of-ikov", "count": 12, "bounds": "ONLY quest-* steps without atom{} between first and last inclusive in route-grand order"}, "micro_prompt": "ATOMIZE quest monoliths, segment 5 of 6 — identical protocol to og-g1 (Bash write-only via > /tmp/og/g5-<n>.out 2>&1; read gotchas [quests]/[items]/[depth:02]/[depth:07]; grep contrib.jsonl oppgran:atoms: first; queue claim/done your oppgran:depth bundle; contribute only, no direct edits). SCOPE: quest-* steps WITHOUT atom{} from quest-the-hand-in-the-sand to quest-temple-of-ikov inclusive in route-grand order (enumerate from /home/lemon/runelite-guide-chain/src/main/resources/fixtures/route-grand.json). Rules: OPPORTUNISTIC_GRANULARITY.md 1b + GRANULARITY.md U1-U10; talk-to per state change; exact-count gather/produce with gather-not-GE alternatives; walk-to at zone transitions only; multi-region phase-split on location.quest_phase; branch{} alternates; U10 ids + coarse_of. Wiki: main-page Details --strip + /Quick guide Walkthrough --strip; manifest-slug-verified refs; flattened requirement chains; counts/coords \"??\" when unsourced; consumes{} slugs on every quest item so demand edges bind. Note: many mid-game quests here carry combat set-pieces — a fight is ONE kill atom with until/count, never per-swing lines (U4); safespot/prayer advice is a hints[] entry (safespot type), never a step. OWN WORDS ONLY. OUTPUT: one contribution per quest keyed oppgran:atoms:<quest-step-id> {key, kind: \"atoms\", coarse_id, steps, checkpoints, refs, notes} via contribute --file. ONE-LINE receipt: 'og-g5: N quests, M atoms, K placeholders'."}
{"id": "og-g6", "wave": 3, "tier": "sonnet", "track": "granularize", "kind": "GENERATE", "scope": {"bundle": "quest monoliths, route order, segment 6 of 6", "first": "quest-rum-deal", "last": "quest-watchtower", "count": 7, "bounds": "ONLY quest-* steps without atom{} between first and last inclusive in route-grand order"}, "micro_prompt": "ATOMIZE quest monoliths, segment 6 of 6 (7 quests) — identical protocol to og-g1 (Bash write-only via > /tmp/og/g6-<n>.out 2>&1; read gotchas [quests]/[items]/[depth:02]/[depth:07]; grep contrib.jsonl oppgran:atoms: first; queue claim/done your oppgran:depth bundle; contribute only, no direct edits). SCOPE: quest-* steps WITHOUT atom{} from quest-rum-deal to quest-watchtower inclusive in route-grand order (enumerate from /home/lemon/runelite-guide-chain/src/main/resources/fixtures/route-grand.json). Rules: OPPORTUNISTIC_GRANULARITY.md 1b + GRANULARITY.md U1-U10; talk-to per state change; exact-count gather/produce with gather-not-GE alternatives; walk-to at zone transitions only; multi-region phase-split on location.quest_phase; branch{} alternates; U10 ids + coarse_of. Wiki: main-page Details --strip + /Quick guide Walkthrough --strip; manifest-slug-verified refs (grep manifest.jsonl for recorded titles — redirect trap); flattened requirement chains per [depth:02]; counts/coords \"??\" when unsourced; consumes{} slugs on every quest item. These late-route quests likely have some blobs already cached from prior depth passes — check manifest.jsonl BEFORE fetching ('in manifest' does not imply 'already extracted', per the [extrapolate] gotcha, but a cache hit costs nothing). OWN WORDS ONLY. OUTPUT: one contribution per quest keyed oppgran:atoms:<quest-step-id> {key, kind: \"atoms\", coarse_id, steps, checkpoints, refs, notes} via contribute --file. ONE-LINE receipt: 'og-g6: N quests, M atoms, K placeholders'."}
{"id": "og-g7", "wave": 3, "tier": "sonnet", "track": "granularize", "kind": "GENERATE", "scope": {"bundle": "train bands, route order, segment 1 of 3", "first": "train-strength-60", "last": "train-hunter-29", "count": 15, "bounds": "ONLY train-* steps between first and last inclusive in route-grand order"}, "micro_prompt": "ATOMIZE train bands, segment 1 of 3 (GENERATE; contribute rows, never edit steps.jsonl/fixtures). Bash write-only via > /tmp/og/g7-<n>.out 2>&1, Read back. Before starting: read gotchas.log [training] entries (the P2P-title, section-0-infobox and synthetic-xp traps), grep contrib.jsonl oppgran:atoms: for claimed work, queue claim your oppgran:depth bundle. SCOPE: train-* steps from train-strength-60 to train-hunter-29 inclusive in route-grand order (enumerate from /home/lemon/runelite-guide-chain/src/main/resources/fixtures/route-grand.json). PER BAND unwind to a ctr-style Faux sequence per OPPORTUNISTIC_GRANULARITY.md 1a: opening bank-loadout withdraw atom (U1; loadout = union of the band's consumes + tools, quantities \"??\" unless sourced), walk-to the training site (U6; tile from the wiki page's {{Map}}/«map» pins, both coordinate formats per the [faux] gotcha, else \"??\"), ONE kill/gather/produce until-atom per method (U4 — never unroll repetition), method alternates as branch{} sharing an alt_group (U9; list order = preference; when: {} until region tags exist), keep-drop/toggle-state/contested-fallback advice as hints[] (never steps), closing deposit atom where the band banks output. produces{} on kill/gather atoms (drops with \"??\" rates) so the wave-4 opportunistic scan can bind them as sources. XP: fetch Experience_table.s2 (cached) once and recompute cumulative(hi)-cumulative(lo) — fixture xp fields are known-bad synthetic constants per the [training] gotcha. Wiki per band: search the real training-page title first ('Pay-to-play X training' does NOT exist for melee/Slayer — the real titles are 'Pay-to-play Melee training' / 'Slayer training'); monster stats live in section 0 — full get --strip, not a numbered section. Refs cite exact section-suffixed manifest slugs. OWN WORDS ONLY; every rate/count sourced or \"??\". OUTPUT: one contribution per band keyed oppgran:atoms:<train-step-id> {key, kind: \"atoms\", coarse_id, steps, checkpoints, refs, notes} via contribute --file; queue done; append new gotchas. ONE-LINE receipt: 'og-g7: N bands, M atoms, K placeholders'."}
{"id": "og-g8", "wave": 3, "tier": "sonnet", "track": "granularize", "kind": "GENERATE", "scope": {"bundle": "train bands, route order, segment 2 of 3", "first": "train-fishing-40", "last": "train-magic-55", "count": 15, "bounds": "ONLY train-* steps between first and last inclusive in route-grand order"}, "micro_prompt": "ATOMIZE train bands, segment 2 of 3 — identical protocol to og-g7 (Bash write-only via > /tmp/og/g8-<n>.out 2>&1; gotchas [training] first; grep contrib.jsonl oppgran:atoms:; queue claim/done; contribute only). SCOPE: train-* steps from train-fishing-40 to train-magic-55 inclusive in route-grand order (enumerate from /home/lemon/runelite-guide-chain/src/main/resources/fixtures/route-grand.json). Unwind per OPPORTUNISTIC_GRANULARITY.md 1a: bank-loadout open (U1), walk-to site with wiki-pinned tile or \"??\" (U6), one until-atom per method (U4), branch{} alternates (U9), advice as hints[] only, closing deposit, produces{} on gather/kill atoms so opportunistic binding works. Skilling-heavy segment notes: production skills (fishing/cooking/smithing/crafting/fletching) unwind to gather->produce atom pairs whose consumes/produces MATH must balance (executions = ceil(need/rate), rates \"??\" until sourced — U2); do-not-fabricate burn/success rates (the [items] gotcha: {{Skilling success chart}} params are chart coordinates, NOT success levels — the real burn-stop table is on Cooking/Burn level); magic training atoms consume runes — name rune slugs and counts-per-cast from the spell's own wiki page or \"??\". XP deltas recomputed from Experience_table.s2 per [training]. Wiki titles: members-skill training pages mostly drop the 'Pay-to-play' prefix (Herblore/Farming training etc. per [training] gotcha) — search first, grep manifest.jsonl for the recorded title before writing refs. OWN WORDS ONLY. OUTPUT: one contribution per band keyed oppgran:atoms:<train-step-id> {key, kind: \"atoms\", coarse_id, steps, checkpoints, refs, notes} via contribute --file. ONE-LINE receipt: 'og-g8: N bands, M atoms, K placeholders'."}
{"id": "og-g9", "wave": 3, "tier": "sonnet", "track": "granularize", "kind": "GENERATE", "scope": {"bundle": "train bands, route order, segment 3 of 3", "first": "train-magic-70", "last": "train-herblore-38", "count": 13, "bounds": "ONLY train-* steps between first and last inclusive in route-grand order"}, "micro_prompt": "ATOMIZE train bands, segment 3 of 3 — identical protocol to og-g7 (Bash write-only via > /tmp/og/g9-<n>.out 2>&1; gotchas [training]+[items] first; grep contrib.jsonl oppgran:atoms:; queue claim/done; contribute only). SCOPE: train-* steps from train-magic-70 to train-herblore-38 inclusive in route-grand order (enumerate from /home/lemon/runelite-guide-chain/src/main/resources/fixtures/route-grand.json). Unwind per OPPORTUNISTIC_GRANULARITY.md 1a with the og-g7/og-g8 rules (U1 loadout open, U6 walk-to with sourced-or-\"??\" tiles, U4 one until-atom per method, U9 branch{} alternates, hints[]-not-steps, closing deposit, produces{} for opportunistic binding, XP from Experience_table.s2). Late-band specifics: these bands sit deep in the route where supply chains already exist — REUSE before authoring (U8): grep steps.jsonl for existing gather/produce rows (brew-prayer-potion, farm-ranarr-patch, gather-*, source-*) and reference their ids inside your expansion instead of minting duplicates; herblore atoms must respect the [items] gotcha (Prayer potion is Herblore 38 per its own {{Recipe}}, base output is the 3-dose potion — cross-check every level/xp against the item page's Creation section, never trust chain details). Where a band's method depends on a quest unlock, express it as reqs.quests on the atom, not prose. OWN WORDS ONLY; counts sourced or \"??\". OUTPUT: one contribution per band keyed oppgran:atoms:<train-step-id> {key, kind: \"atoms\", coarse_id, steps, checkpoints, refs, notes} via contribute --file. ONE-LINE receipt: 'og-g9: N bands, M atoms, K placeholders'."}
{"id": "og-g10", "wave": 3, "tier": "sonnet", "track": "granularize", "kind": "GENERATE", "scope": {"bundle": "travel/navigation pass over the whole route", "bounds": "ONLY steps classified travel-implicit by wave 1 (grep contrib keys oppgran:classify:s1..s4); no quest/train atomizing — those belong to og-g1..g9"}, "micro_prompt": "TRAVEL-ATOM pass (GENERATE; contribute rows, never edit steps.jsonl/fixtures). Bash write-only via > /tmp/og/g10-<n>.out 2>&1, Read back. Before starting: read gotchas.log [travel] and [faux] entries (parent-pulls-children section fetches; two {{Map}} coordinate formats; fairy-ring section-offset trap); grep contrib.jsonl for oppgran:classify:s1..s4 and extract every row with grain_class travel-implicit into /tmp/og/g10-targets.out; grep oppgran:atoms: to skip claimed work; queue claim your oppgran:depth bundle if one names this track. SCOPE: exactly those travel-implicit steps — do NOT atomize quest/train content (og-g1..g9 own it; if a travel fix belongs inside another agent's quest expansion, contribute a note row oppgran:travel-note:<step_id> instead of atoms). PER TARGET: extract the zone transition buried in the step's prose and emit walk-to/teleport atoms per U6 (transitions only; same-zone movement stays in the host atom's detail): atom verb walk-to or teleport, target zone/gate slug, tile from the destination page's {{Map}}/«map» pins (both formats) or \"??\", consumes{} for tolls/runes/charges (sourced or \"??\"), teleport-choice hints[] ranking transport alternatives (csv, prefer-first, per GRANULARITY section 4), and a branch{} only when transport alternatives change the route shape (U9). Fairy-ring codes: grep the cached Fairy_ring blobs for the specific code rather than trusting section order (the [faux] offset gotcha). Refs cite exact section-suffixed manifest slugs; grep manifest.jsonl for recorded titles first. OWN WORDS ONLY. OUTPUT: one contribution per target keyed oppgran:atoms:<step_id> {key, kind: \"atoms\", coarse_id: <step_id>, steps, refs, notes} via contribute --file; queue done; append new gotchas. ONE-LINE receipt: 'og-g10: N travel targets, M atoms, K placeholders'."}
{"id": "og-o1", "wave": 3, "tier": "sonnet", "track": "opportunistic-map", "kind": "GENERATE", "scope": {"bundle": "opportunity grounding, shortlist ranks 1..N/2 (top half)", "bounds": "ONLY items assigned by og-p1's oppgran:depth queue to opportunistic-map track, top half of the ranked shortlist; read-only on all route/step data"}, "micro_prompt": "GROUND OPPORTUNITIES, top half of the ranked shortlist (GENERATE; contribute rows, never edit steps.jsonl/fixtures). Bash write-only via > /tmp/og/o1-<n>.out 2>&1, Read back. Before starting: read gotchas.log [npcs]+[bg]+[items] entries (shop-stock via the NPC page's own Stock section; {{Drop sources}}/{{Store locations list}} are unexpandable Cargo stubs — use links '<Item>' then the NPC page; {{ItemSpawnLine}} Spawns subsections are the wikitext-literal source); grep contrib.jsonl for oppgran:opp: to skip claimed items; queue claim your bundle. SCOPE: the TOP-HALF items of og-p1's opportunity shortlist (grep contrib key oppgran:depth-queue). PER ITEM: ground the earliest-in-position source: (1) confirm the non-GE source (gather spawn / producible / named shop NPC — never Grand Exchange); (2) pin the source location: zone slug + tile from {{Map}}/«map»/{{NPC map}} pins (the NPC's own page, not the shop page) or \"??\"; (3) record trigger: {zones: [...], hubs: [...]} — every zone where the action is executable, so the wave-4 scan has the full window, plus min_skills if the gather needs a level; (4) name the payoff consumer (step/goal id from the oppgran:opp-surface rows) and qty (sourced or \"??\"); (5) if NO steps.jsonl producer row exists, propose one: full steps.jsonl-shaped row under a PROPOSED-ID (gather-<item>-<zone>), flagged proposed:true — per the [bg] gotcha a missing row is a gap to fill, not a skip. OUTPUT: one contribution per item keyed oppgran:opp:<item>@<zone> shaped {\"key\", \"kind\": \"opportunity\", \"item\", \"source_step\": \"id-or-PROPOSED\", \"proposed_row\": null-or-row, \"trigger\": {\"zones\", \"hubs\", \"min_skills\"}, \"payoff\": {\"consumer\", \"goal\", \"qty\"}, \"refs\": [...], \"notes\": \"own words\"} via contribute --file. Refs cite exact manifest slugs (grep manifest.jsonl for recorded titles — redirect trap). OWN WORDS ONLY; no fabricated tiles/quantities. queue done; append new gotchas. ONE-LINE receipt: 'og-o1: N items grounded, M proposed rows, K placeholders'."}
{"id": "og-o2", "wave": 3, "tier": "sonnet", "track": "opportunistic-map", "kind": "GENERATE", "scope": {"bundle": "opportunity grounding, shortlist ranks N/2+1..N (bottom half)", "bounds": "ONLY items assigned by og-p1's oppgran:depth queue to opportunistic-map track, bottom half of the ranked shortlist; read-only on all route/step data"}, "micro_prompt": "GROUND OPPORTUNITIES, bottom half of the ranked shortlist — identical protocol to og-o1 (Bash write-only via > /tmp/og/o2-<n>.out 2>&1; gotchas [npcs]+[bg]+[items] first; grep contrib.jsonl oppgran:opp: for claimed items; queue claim/done; contribute only). SCOPE: the BOTTOM-HALF items of og-p1's opportunity shortlist (grep contrib key oppgran:depth-queue; skip any item og-o1 already contributed). PER ITEM: confirm the non-GE source (gather/produce/named shop NPC); pin zone + tile via {{Map}}/«map»/{{NPC map}} on the right page (NPC page for shopkeepers, not the shop page) or \"??\"; record trigger {zones, hubs, min_skills}; name payoff consumer + qty from the oppgran:opp-surface rows; propose a full steps.jsonl-shaped PROPOSED-ID row when no producer exists (flag proposed:true). Bottom-half items are lower-payoff — where grounding shows the in-position window is ILLUSORY (source zone never actually precedes the consumer in route order, or the gather needs skills the route grants later than the window), say so explicitly in notes with verdict 'no-window' instead of forcing a weave: a documented no-window verdict is a valid deliverable that saves wave-4 scan time. OUTPUT: one contribution per item keyed oppgran:opp:<item>@<zone> shaped as in og-o1 ({key, kind: \"opportunity\", item, source_step, proposed_row, trigger, payoff, refs, notes}) via contribute --file. Refs cite exact manifest slugs. OWN WORDS ONLY; \"??\" over any guess. ONE-LINE receipt: 'og-o2: N items grounded, M no-window verdicts, K proposed rows'."}
{"id": "og-w1", "wave": 4, "tier": "sonnet", "track": "granularize", "kind": "FIX", "scope": {"files": ["/home/lemon/osrs-wiki/assets/data/tools/steps.jsonl", "/home/lemon/osrs-wiki/assets/data/tools/coarse_expansions.jsonl"], "bounds": "apply oppgran:atoms:* and oppgran:opp:* contributions to the two data files; NO planner/enrich code changes (og-w2 owns those)"}, "micro_prompt": "CONSOLIDATE DATA (FIX; this task edits the two scope data files and nothing else — og-w2 owns all code). Bash write-only via > /tmp/og/w1-<n>.out 2>&1, Read back. Before starting: read gotchas.log [consolidate] entries (registry-stable checkpoint ids; coordinate-envelope bands incl. dungeon y+6400 and instanced y4000-6500; legacy-ref rebinding); grep contrib.jsonl for every oppgran:atoms: and oppgran:opp: key into /tmp/og/w1-keys.out. TASK: (1) For each oppgran:atoms:<coarse_id> contribution: append its atom rows to steps.jsonl (skip ids that already exist — never mint duplicates, per the [materialize] gotcha enrich existing rows instead), upsert the coarse_expansions.jsonl row {coarse_id, name, status: \"authored\", steps: [ordered ids], checkpoints} preserving registry order; set coarse_of on every atom. (2) For each oppgran:opp: contribution with a proposed_row: append it under its PROPOSED-ID; stamp authored opp{} triggers onto source rows where the contribution's trigger zones differ from the row's own location.zone. (3) LINT (fail-loudly to /tmp/og/w1-lint.out): every atom.verb in the GRANULARITY section 1b 16-verb enum; every hints[].type in the section 4 enum; every checkpoints[].start resolves into its own steps[]; every refs[] slug greps in tools/wiki-kb/manifest.jsonl (title-redirect + .sN traps); coordinate plausibility per the [consolidate] envelope; bare-number grep over produces/consumes (every number sourced-or-\"??\" per contribution notes); every branch{}.alt_group has 2+ members or optional:true. Quarantine failing rows to /tmp/og/w1-quarantine.jsonl with reasons — never silently drop or silently fix. (4) Run the jsonl validity check (python3 json.loads per line) over both files. Own words preserved verbatim from contributions — you consolidate, you do not rewrite prose. Commit is NOT yours to make — leave the working tree dirty and report. ONE-LINE receipt: 'og-w1: N atom rows applied, M coarse rows upserted, K quarantined (reasons in file)'."}
{"id": "og-w2", "wave": 4, "tier": "sonnet", "track": "weave", "kind": "GENERATE", "scope": {"files": ["/home/lemon/osrs-wiki/assets/js/router/planner/burndown.js", "/home/lemon/osrs-wiki/tools/guide-export/enrich.py", "/home/lemon/runelite-guide-chain (GuideStep.java + fixture consumers)"], "bounds": "ONLY the _payoff stamp, the P8 opportunistic placement scan + fallback stubs, and the paysOff emit/plugin field; no data-file edits (og-w1 owns those)"}, "micro_prompt": "IMPLEMENT the opportunistic-lookahead code seams per /home/lemon/osrs-wiki/tools/guide-export/design/OPPORTUNISTIC_GRANULARITY.md section 2 (read it first — it is the spec; also read SYNTHESIS.md sections 2-3 for the pass contract and GRANULARITY.md section 3c for branch{}). Bash write-only via > /tmp/og/w2-<n>.out 2>&1, Read back. Scope is code-only: (1) burndown.js — stamp _payoff {consumer, goal, item} on every injected supply step at resolve time (the consumer is already known inside resolveStepDeps/resolveChain/burndownResolve; keep the edge instead of dropping it; ~10 lines). (2) enrich.py P8 — the backward demand-propagation engine is ALREADY LANDED and run-proven at /home/lemon/osrs-wiki/assets/js/router/planner/backprop.js (buildSourceIndex/collectDemands/accumulateSkills/backpropCollectionPlan; spike at tools/guide-export/spikes/backprop-spike.mjs — run it first to see real output). Your job is WIRING, not reimplementing the sweep: extend insert_supply_steps (today annotate-only) to consume backprop plans (port the sweep to Python or run the JS engine as a pre-pass whose plans ride the export JSON — pick one, document why); for each earliest-window plan re-pin the source step at the collection node via the existing _anchor/_side detach/reattach contract with _opportunistic:true, emit the optional:true ITEM_HELD fallback stub sharing alt_group opp-<item>-<consumer>, surface sourceAfterConsumer flags as lint output (route data faults — never silently reorder), re-run topo_order afterward as the guard; already-earliest/no-window plans = today's behavior byte-identical. (3) Emit paysOff {at, item} on opportunistic steps in the step emitters; GuideStep.java gains public GuidePayoff paysOff (String at; String item;) — additive, unknown-field-safe. HARD RULES: never break topo/requisite gating (the re-run is mandatory); overlay-only rendering; no GE semantics anywhere; named tuning constants over magic numbers; max one indent level, guard clauses, lookup tables (project conventions). TESTS: npm test in /home/lemon/osrs-wiki must stay green; routes without _payoff data must export byte-identical (regression diff on an untouched fixture); add a planner test: a demand with an early in-window source weaves at the window with a fallback stub, and a no-window demand is unchanged. Baseline re-pins are intentional acts — note any in /tmp/og/w2-repins.out with justification. Do not commit — leave the tree dirty and report. ONE-LINE receipt: 'og-w2: payoff stamp + P8 scan + paysOff emit landed, tests green, N baselines re-pinned'."}
{"id": "og-w3", "wave": 4, "tier": "sonnet", "track": "weave", "kind": "FIX", "scope": {"files": ["/home/lemon/runelite-guide-chain/src/main/resources/fixtures/route-grand.json"], "bounds": "re-export + verify only; fix nothing outside export inputs; escalate code faults to og-w2, data faults to og-w1"}, "micro_prompt": "RE-EXPORT + VERIFY route-grand (FIX/verify; runs after og-w1 and og-w2 land). Bash write-only via > /tmp/og/w3-<n>.out 2>&1, Read back. TASK: (1) run the export pipeline (tools/guide-export/plan-multi.mjs | enrich.py per the repo's export convention — read the header comment in enrich.py for the exact invocation) writing the new route-grand fixture to a scratch path first; (2) diff against the current fixture into /tmp/og/w3-diff.out and Read it; (3) VERIFY GATES: valid JSON; step count strictly greater than 205 (atoms landed); every _opportunistic/paysOff step has a matching oppgran:opp: contribution (grep contrib.jsonl) AND sits earlier in the route than its payoff consumer; every fallback stub shares alt_group with exactly one opportunistic sibling and is optional; zero steps lost relative to the old route except generalized rows now represented by their coarse_of atoms; checkpoint headers use registry-stable ids (the [consolidate] gotcha); no wiki prose leaked into labels/details (spot-check 10 random new atoms against their cited blobs — paraphrase only); npm test green. (4) Classify every diff hunk as intended (atoms/weaves per design) or regression; regressions go to /tmp/og/w3-regressions.out with the owning task (og-w1 data / og-w2 code) — do NOT fix them yourself. (5) If all gates pass, install the new fixture at the scope path and re-pin any test baselines as an explicit intentional act listed in your receipt. Do not commit — leave the tree dirty and report. ONE-LINE receipt: 'og-w3: route-grand N steps (was 205), M opportunistic weaves verified, K regressions escalated, baselines re-pinned: [list]'."}
